# Astra Agent Reliability Harness 严重问题整改与业务验收方案

> 审计基线：`5f225b95ab2236f9a581256227b2b7f226473535` 及审计时工作区实际内容
> 适用范围：Phase 2 生产入口、Phase 3 Governance、Phase 4 Durable Task Runtime，以及相关状态声明
> 权威优先级：Phase 4 冻结文档（P0）> Phase 3 冻结文档（P1）> 架构与 Phase 2 语义（P2）
> 文档目的：将审计发现转化为可实施整改项和以真实业务可用性、稳定性、设计契合度为裁决依据的验收合同。

本方案不把单元测试、测试数量、覆盖率、pytest 结果、fixture 结果或现有测试预期视为正确性证据。任何能力只有通过正式业务入口、正式运行组装、权威持久化状态和实际业务系统结果得到闭环证明，才能判定为通过。无法执行、无法观察、无法回链、只能依赖模拟或证据互相矛盾的项目，一律不得判定为通过。若冻结设计本身没有定义可裁决语义，必须先补充或变更权威设计，再修改 Production 和测试；测试不得自行发明预期替设计填空。

# 第一部分：具体问题

## P-01：生产链存在多套权威对象，任务生命周期不能形成单一闭环

当前同时存在 `executions` 与 `phase3_executions`、`interactions` 与 `phase3_interactions`。Hermes Adapter 写入 legacy 表，Governance 从 Phase 3 表读取；Governance 的 `continue_with_feedback` 只派生 `phase3_executions`，Worker 却只消费 `phase4_run_requests`。因此“执行结束—治理决策—等待或继续—新执行”不是同一条可恢复链。

直接后果包括：Task 保持 `running` 但没有可消费工作项；Interaction 已创建但 Runtime 无法解析；治理记录存在但 Worker 永远看不到；重启后无法确定哪一套表是权威事实。该问题违反 ADR-003 的单一 Task truth、ADR-004 的 AstraStore 唯一权威边界，以及 Phase 4 `TASK_RUNTIME.md` 第 2、4、7、8、9 节。

## P-02：Task Contract 的工具、Schema、能力、版本和约束没有在真实 Gateway 完整执行

当前 Gateway 主要校验工具名、`allowed_tools` 和硬编码 Pydantic 参数类型，没有按照持久化 Task Contract 校验 capability、resolved tool version、contract schema hash、版本化 constraint handler、authorized effect intent 和作用域。生产代码无法证明实际工具调用与 Task 创建时冻结的 Contract 完全一致。

结果是 Contract 可能只作为上下文信息存在，不能成为强制执行边界；未知约束、版本漂移或 schema 漂移可能被静默接受。该问题违反 `TASK_CONTRACT.md` 第 5—10 节和 ADR-003 的 Constraint Boundary。

## P-03：正式副作用治理链未接入真实 Tool Gateway，存在 fail-open

真实调用链仍是 `Hermes Bridge → AstraToolGateway → Business Adapter → execution_receipt`。CanonicalEffectRequest、Normalizer、exact Approval、ExternalOperation、派生幂等键、authoritative confirmation 只存在于可调用的治理 API 中，没有成为副作用工具的必经路径。

`ExecutionBridgeContext` 直接接受模型提供的 `idempotency_key`，Gateway 可在不存在权威 Task、Approval 和 ExternalOperation 的情况下创建业务对象。数据库虽然定义了 ExternalOperation 唯一性，但真实生产链绕过该表，因而无法阻止跨 Execution、Attempt、Worker 或进程的重复副作用。该问题违反 `EFFECT_IDENTITY_AND_IDEMPOTENCY.md` 第 1—9 节、`APPROVAL_BINDING.md` 第 6—8 节和 Phase 4 P4-A12 的设计意图。

## P-04：取消事实不能阻止迟到副作用，也不能可靠处理迟到结果

TaskRuntime 缺少完整的 `cancel_task` 生产命令；Gateway 不读取 Task、Attempt、Execution 的当前权威状态；`prepare_external_operation` 也没有在同一权威事务内完成取消状态校验。审计探针已经证明 Task/Attempt 被标记为 `cancelled` 后仍可创建 `prepared` ExternalOperation。

这意味着取消只是局部状态标签，不是全链路强约束。迟到 Worker、旧 Execution 或旧模型调用仍可能产生外部效果，终态也可能被迟到结果污染。该问题违反 `TASK_RUNTIME.md` 第 10 节和 P4-A05/P4-A06 的不变量。

## P-05：waiting_input 写入错误权威表，恢复路径不可用

Hermes 路径把 Interaction 写入 legacy `interactions`，Governance 读取 `phase3_interactions`。实际发生输入等待后，Task 仍可能保持 `running`，Governance 看不到 pending Interaction；某些分支还因缺少 `interaction_spec` 抛错，使 Execution 保持 `running`、Run Request 保持 `claimed`。

当前状态既不是可执行状态，也不是可恢复等待状态，属于永久悬挂。该问题违反 `TASK_LIFECYCLE.md` 第 5 节和 `TASK_RUNTIME.md` 第 9.1 节。

## P-06：waiting_approval 虽能拒绝未授权操作，但拒绝后无法恢复

治理 API 内部的 approval binding 校验较完整，但生产 Gateway 没有接入；系统也缺少完整的 ApprovalRequest、Interaction、ApprovalResolution、resolve 和新 Run Request 生产链。缺审批时虽然阻止了错误 completion，却可能同时结束 Execution 和原 Run Request，将 Task 留在 `running` 且没有 Interaction、没有下一 Run Request 的状态。

这不是安全完成态，而是“fail-closed 但不可恢复”。同时，主动审批与 Gateway 强制审批尚未证明汇合为同一条 Runtime 审批路径。该问题违反 `APPROVAL_BINDING.md` 第 3—9 节和 `TASK_RUNTIME.md` 第 9.2 节。

## P-07：continue_with_feedback、start_new_attempt 和次数上限没有生产执行链

`continue_with_feedback` 的治理应用没有原子创建 Worker 可消费的 `phase4_run_requests`；默认 Policy 映射无法证明会选择 `start_new_attempt`；`max_attempts`、`max_executions_per_attempt`、`max_feedback_cycles`、`max_reconcile_cycles` 和 Attempt deadline 没有贯穿真实运行链。

系统因此可能出现“决策已经保存但永不继续”、无限循环、错误复用 Attempt 或永不触发新 Attempt。该问题违反 `TASK_POLICY.md` 第 2、5、7 节，`TASK_CONTRACT.md` 第 9 节和 `TASK_RUNTIME.md` 第 4.2、8、13 节。

## P-08：Completion Evaluator 可把不相关副作用误判为满足，产生 false success

当前 `AuthorizedEffectConfirmedEvaluator` 没有严格使用 requirement configuration 绑定具体 effect intent、subject、authority domain、规范化参数和 authoritative observation。一个 confirmed operation 可能满足多个本应独立的 required requirements。审计探针已证明两个 required effects 仅完成一个时，Completion 仍可得到 `satisfied` 并把 Task 标记为 `succeeded`。

这会直接产生业务层假成功，是当前最高风险之一。该问题违反 `REQUIREMENT_EVALUATORS.md` 第 3—8 节、`EFFECT_IDENTITY_AND_IDEMPOTENCY.md` 第 9 节和 `DECISION_POINTS_AND_SNAPSHOTS.md` 第 7 节。

## P-09：EvidenceSnapshot 未覆盖全部权威版本，权威状态变化后不能稳定重放

当前 Snapshot identity 主要包含 execution/result hash 和 Task/Attempt version，没有完整纳入 ExternalOperation、Interaction、Approval、Receipt、Fact watermark、业务 Observation 等会影响裁决的权威版本。ExternalOperation 从 `prepared` 变为 `confirmed` 后重新求值，可能命中旧 identity 并产生 CompletionValidation identity conflict。

这意味着同一业务事实变化不能产生明确的新 Snapshot，同一 Snapshot 也不能保证对应固定事实集合，破坏了确定性重放和审计回溯。该问题违反 `RELIABILITY_FACTS_AND_EVIDENCE.md` 第 7—9 节和 `DECISION_POINTS_AND_SNAPSHOTS.md` 第 2、4、8 节。

## P-10：生产 composition root 未注册标准 Evaluator/Normalizer/Constraint Handler

`TaskRuntime` 默认创建空 evaluator registry，标准 evaluator 主要由 tests、fixture 或 Round 2 辅助路径手工注册。真实任务即使已有 confirmed operation，也可能得到 `unknown_evaluator/evaluator_error → escalate → Task failed`。同类风险也适用于 normalizer、constraint handler、rule 和 policy 组件。

这说明领域组件“代码存在”不等于“生产能力可用”。该问题违反 `REQUIREMENT_EVALUATORS.md` 第 5 节、`TASK_RULES.md` 第 4 节和 Phase 4 单 Worker 生产组装要求。

## P-11：deadline/budget 会留下不可消费的 pending Run Request，毒化调度队列

deadline 更新 Task/Attempt 为终态时没有同步终结或取消关联 Run Request。调度器之后仍会优先选中该请求，得到 `task_not_executable`；如果选择逻辑不能跳过或清理毒请求，后续有效任务会饥饿。

当前实现只证明某个任务被标记失败，没有证明队列仍可持续服务其他任务。该问题违反 `TASK_RUNTIME.md` 第 7、13 节和 P4-A13 的实际可用性要求。

## P-12：Executor 或 Worker 异常会遗留 running Execution、claimed Run Request 和 running Task

`SingleWorker.run_once` 对 `executor.execute` 的异常缺少统一终结、事务补偿和恢复分类。Executor 抛错后，Execution、Run Request 和 Task 可能长期停留在非终态；直接 `HermesExecutor.execute` 入口还因不再创建 Execution，首次 Trace 写入即可触发外键失败。

该问题既破坏 Worker 闭环，也构成 Phase 2 公开入口回归。它违反 Phase 2 Adapter 终结约束、`TASK_RUNTIME.md` 第 8、12 节和 Execution exactly-once 终结语义。

## P-13：Checkpoint、restart recovery、orphan scanner 和 reconciliation runner 未实现

当前没有完整 checkpoint 权威表、启动扫描器、orphan classifier、recover command、恢复 Run Request 生成器和 reconciliation runner。进程被强杀后，系统无法确定性地区分未开始、已运行、可能已 dispatch 外部操作、已保存结果但未治理等 crash window。

因此 Phase 4 最核心的 Durable Runtime 目标尚未闭合。该问题违反 ADR-004 第 3、4 节和 `TASK_RUNTIME.md` 第 11、12 节。

## P-14：Run Request 缺少 lease、heartbeat 和 fencing，无法安全支持多进程

Run Request schema 没有 owner、不可复用 lease token、expiry 和 heartbeat；result/finalize 不校验当前 token。普通事务 claim 只能减少同时 claim 的概率，不能阻止 lease takeover 后的旧 Worker 继续写入。

这会造成双执行、旧结果覆盖新结果、重复外部副作用和错误终结。该问题违反 ADR-004 第 5 节和 `TASK_RUNTIME.md` 第 14 节。

## P-15：Facts、Outbox 和 Task Rules 只具备局部写入能力，没有运行闭环

Governance 可原子写 Fact/Outbox，但缺少 Dispatcher、幂等重投、startup scan 和可持续的 Rule runner。真实 Gateway 副作用也不发布与 ExternalOperation、业务观察一致的治理事实。

因此审计记录不能可靠传播，跨 Attempt 无进展、迟到状态、recovery divergence 和 receipt/business mismatch 等规则没有生产触发点。该问题违反 `RELIABILITY_FACTS_AND_EVIDENCE.md` 第 5—8 节和 `TASK_RULES.md`。

## P-16：现有测试预期反向塑形实现，并掩盖生产缺陷

现有测试中存在手工 `start_execution/finalize_execution`、手工注册 evaluator、fake executor 直接创建 ApprovalResolution/ExternalOperation 并置为 confirmed、把永久悬挂状态写成预期、把毒化 Run Request 保持 pending 写成预期等做法。部分 Round 2 测试还直接调用 production contract chain，失去独立 Oracle 能力。

这些测试只能说明“当前实现符合当前测试”，不能说明业务代码符合冻结设计。继续以此作为 Gate 会固化缺陷，违反独立裁决、权威事实和不可验证不得通过的审计原则。

## P-17：项目状态声明高于实际生产完成度

README/状态文档宣称 Phase 3 complete，而生产端到端治理链仍存在 fail-open、false success、不可恢复等待和缺失组装；Phase 4 当前仅形成 submit/persistence 和单 Worker happy-path 片段；Phase 5/6 没有 production 入口和评估闭环。

在真实业务验收完成前继续使用 `COMPLETE`、`FROZEN` 或 `production ready` 会误导发布和风险决策。

## P-18：稳定性验收缺少由需求或设计冻结的量化标准

现有冻结文档定义了 crash window、并发不变量、生命周期和安全边界，但没有统一冻结运行时长、任务规模、故障比例、恢复时限、队列排空时限或资源上限。若测试自行规定“运行多少小时、执行多少次即通过”，测试就越权替设计创建了非功能需求；若完全不做稳定性验证，又无法证明实际可用性。

冻结设计缺少稳定性量化验收标准，因此生产稳定性目前不可裁决。需设计方补充正式设计后，再开展对应整改和验收。

# 第二部分：解决方案

本部分只能恢复冻结设计已经规定的 Production 语义。整改必须修改业务代码、生产组装或正式运行链；测试只能驱动和观察，不能补偿、构造或推进生命周期。原设计未规定或存在冲突的内容必须标记为 `DESIGN GAP` 或 `BLOCKED`，不得自行实现。

## S-01：恢复唯一权威存储与既有职责边界

### 原设计

> ```text
> AstraStore
> = 唯一权威持久化与本地事务边界
> ```

> - Astra Task Runtime 负责 Task、Attempt、Execution、Interaction、Run Request、
>   cancel、checkpoint、recovery 和 Worker ownership；
> - RuntimeGovernanceCore 继续负责 Task Policy、Completion Gate、治理决策及其
>   CAS 应用；
> - AstraStore 继续承载权威记录、幂等约束和原子事务；

> Phase 4 不修改 Hermes Agent Loop，不复制 RuntimeGovernanceCore，不创建第二套
> Runtime authority，也不把 Task 生命周期实现为 Workflow、DAG 或固定业务步骤。

> 不得创建平行的 Phase 4 authority store、第二套 Task 状态机或通过消息投递记录
> 反推当前 Task 状态。Schema 使用显式版本并原地迁移；迁移失败时 Worker 不得
> 开始执行。

### 原设计来源

- 冻结点：`phase4-p4.0-contract-frozen`
- `docs/phase4/TASK_RUNTIME.md`，blob `424ae6bada383d4b318406f436afc4f5c6a7b83f`，第 2 节“架构边界”，冻结原文第 45—77 行。
- `docs/adr/ADR-004-persistent-task-runtime.md`，blob `9a057a323e94ecb38114f58872d8f8aa9085c24a`，Decision 1“AstraStore 是唯一权威存储边界”，冻结原文第 21—28 行。

### 当前 Production 偏差

Production 同时保留并写入 `executions`/`interactions` 与 `phase3_executions`/`phase3_interactions`。Hermes Adapter、Runtime Worker 与 Governance 分别依赖不同对象，导致 Execution、Interaction 和继续执行的事实没有唯一权威来源。Governance 创建的派生 Execution 也不是 Worker 消费的 Run Request，生命周期无法形成单一闭环。

### 对应业务代码位置

- `astra/storage.py:160-197`：legacy `executions`、`interactions`。
- `astra/phase3/governance.py:200-223`：`phase3_executions`、`phase3_interactions`。
- `astra/runtime.py:268-279`：Worker 只从 `phase4_run_requests` 选择工作。
- `astra/phase3/governance.py:1535-1550`：`continue_with_feedback` 派生 `phase3_executions`，未创建 Run Request。

### 之前为什么错

此前把问题主要表述成“缺少唯一 composition root”，但冻结设计要求的是唯一权威存储、唯一 Runtime authority 和既有职责边界；“新增 composition root”并不是原设计中的独立产品语义。把组装形式当作根因，会掩盖双表、双写和双状态机才是实际违约点。

### 正确整改方法

只恢复冻结设计规定的唯一权威语义：确定 Task、Attempt、Execution、Interaction、Run Request、Approval、ExternalOperation、Receipt、EvidenceSnapshot、Fact 和 Outbox 的唯一 Production 写路径；将所有正式入口、Runtime、Governance、Gateway 和恢复流程接到同一 AstraStore 事务边界。对重复表执行原地兼容迁移并停止双写，不新增第二套状态机、同步器或镜像权威。现有入口必须复用该权威链，不以测试组装替代 Production 组装。

### 整改完成判据

- 从正式 Task 入口开始，Production 独立创建并推进 Task、Attempt、Run Request、Execution 和 Interaction，所有对象只有一个权威 identity/version/state 来源。
- waiting、continue、new attempt、cancel、completion 和 restart 后的下一生命周期均由同一权威事务链完成，不存在一张表已变化而另一张表仍保持旧状态。
- 数据库关闭重开后，正式 Runtime 仅依据权威记录恢复；不需要测试直接写库、手工复制状态或同步两套表。
- 任一正式路径仍向两套 Execution/Interaction 权威表写入，或出现 running Task 无权威工作项，即判定整改失败。
- 只有 Production 完整闭环和权威证据均可验证时才可判定完成；无法验证为 `BLOCKED`，不得判定完成。

## S-02：恢复 Task Contract 在 Gateway 的强制执行

### 原设计

> Gateway 必须同时校验 capability、tool identity 和 schema identity。Tool/plugin 升级导致 schema hash 变化时，旧 Task 不得静默采用新语义；必须经过兼容性解析或创建新 Contract version。

> 未知 constraint、未知版本或无法加载 handler 时，受影响动作必须 fail closed。

> 所有有外部副作用的动作都必须匹配一个 `authorized_effects[]` 条目。

> 副作用调用前，Effect Normalizer 必须证明请求匹配且只匹配一个 effect intent。零匹配或多重匹配都必须拒绝。

### 原设计来源

- 冻结点：`phase3-governance-complete`
- `docs/phase3/TASK_CONTRACT.md`，blob `aa64732150e6b9d8c41881e17d6a6f49517db048`，第 5 节“Capability and resolved tool boundary”、第 6 节“Deterministic constraints”、第 7 节“Authorized effect intents”，冻结原文第 164—225 行。

### 当前 Production 偏差

Gateway 只检查工具是否注册、工具名是否位于调用上下文的 `allowed_tools`，并用本地硬编码 Pydantic 模型校验参数。它没有从权威 Task Contract 校验 capability、tool version、schema hash、constraint ID/version、handler、subject/scope 和 authorized effect intent，也无法阻止旧 Task 静默采用新工具语义。

### 对应业务代码位置

- `astra/tool_gateway.py:55-71`：工具和参数模型在 Gateway 内硬编码注册。
- `astra/tool_gateway.py:77-103`：只检查工具名、`allowed_tools` 和本地 Pydantic schema。
- `astra/hermes_adapter/context.py:79-85`：调用上下文直接使用 Invocation 提供的 `allowed_tools`，没有加载权威 Contract binding。

### 之前为什么错

此前只抽象地写“加强 Gateway”，没有逐项恢复冻结设计明确规定的 capability、tool identity、schema identity、constraint handler 和 authorized effect intent 五类强制边界；这会让局部白名单和硬编码参数模型继续被误认为已经满足 Task Contract。

### 正确整改方法

每次正式 Tool 调用都必须由 Production 从权威 Task/Attempt/Execution 加载精确 `contract_id`、`contract_version`、`contract_hash` 和 resolved tool binding；依次校验 capability、tool name/version、schema hash、access mode、subject/scope 和所有版本化 constraint handler。read tool 只有通过上述边界后才能进入业务 Adapter；effect tool 还必须唯一匹配 `authorized_effects[]` 并进入 Canonical Effect 链。未知、缺失、歧义或版本漂移全部 fail closed，不依赖 Prompt、模型自律或测试预置列表。

### 整改完成判据

- 通过正式 Hermes→Bridge→Gateway 生产路径，完全匹配冻结 Contract 的调用能够进入实际业务 Adapter。
- capability、tool identity、tool version、schema hash、constraint ID/version、subject/scope 或 effect intent 任一不匹配时，Production 在业务调用前确定性拒绝并保留结构化原因。
- 旧 Contract 遇到新 schema/tool version 时不得静默执行；必须遵守冻结设计的兼容解析或新 Contract version 语义。
- 未知 constraint/handler、零 effect intent 匹配或多重匹配不得产生 Receipt、ExternalOperation 或实际业务副作用。
- 成功不得依赖测试修改 `allowed_tools`、直接调用 Gateway、monkeypatch schema、fixture 或硬编码成功返回；否则整改无效。

## S-03：恢复 Canonical Effect、Approval 与 ExternalOperation 正式副作用链

### 原设计

> TaskContract.authorized_effect  
> → raw tool request  
> → versioned Effect Normalizer  
> → CanonicalEffectRequest  
> → effect_identity  
> → Approval binding, if required  
> → ExternalOperation  
> → idempotency key  
> → Receipt / Evidence / reconciliation

> 每个有副作用的外部调用必须在调用前建立或复用 ExternalOperation。

> `UNIQUE(authority_domain, effect_identity)`

> 副作用调用前顺序固定：
>
> ```text
> 1. validate Task Contract and resolved tool/schema
> 2. normalize raw request
> 3. uniquely match authorized effect intent
> 4. compute effect_request_hash and effect_identity
> 5. find applicable ApprovalRequirement
> 6. if required, load and verify ApprovalResolution/Credential
> 7. atomically reserve/consume approval usage and prepare ExternalOperation
> 8. call external authority
> ```

### 原设计来源

- 冻结点：`phase3-governance-complete`
- `docs/phase3/EFFECT_IDENTITY_AND_IDEMPOTENCY.md`，blob `d3eb1bbb3d464241b6a357394902991786d04a0a`，第 1 节“Core invariant”、第 6 节“ExternalOperation identity”、第 7 节“Operation state and crash windows”，冻结原文第 6—34、184—237 行。
- `docs/phase3/APPROVAL_BINDING.md`，blob `7e5c2e1577c50893ca4e8ace6455677f8f13fc9f`，第 6 节“Gateway match algorithm”、第 7 节“Atomic consumption and ExternalOperation”，冻结原文第 146—203 行。

### 当前 Production 偏差

Bridge 直接接受并传递模型提供的 `idempotency_key`；Gateway 对副作用工具只要求该 key 存在，随后直接调用业务 handler 并写 legacy Receipt。正式 Normalizer、CanonicalEffectRequest、exact Approval 和 `prepare_external_operation` 没有成为真实 Gateway 的必经路径，因此 ExternalOperation 唯一约束、审批绑定、取消检查和 authoritative confirmation 都可被绕过。

### 对应业务代码位置

- `astra/hermes_adapter/context.py:70-86`：从模型参数中取出并接受 `idempotency_key`。
- `astra/tool_gateway.py:105-135`：副作用调用直接进入业务 handler 和 `execution_receipts`。
- `astra/phase3/governance.py:946-1035`：正式 Approval/ExternalOperation prepare 仅作为孤立 Governance API 存在，Gateway 未接入。

### 之前为什么错

此前把“领域组件已经存在”和“Production 副作用链已经治理”混为一谈，并容易用 Receipt 唯一索引或模型提供的幂等键证明正确；这绕过了冻结设计规定的 canonical identity、exact approval、ExternalOperation 和远端权威确认。

### 正确整改方法

将所有正式 effect tool 的唯一 Production 路径恢复为冻结链：加载并验证 Task Contract → 版本化 Normalizer → 唯一匹配 authorized effect intent → 生成 CanonicalEffectRequest/effect identity → exact Approval 校验 → 同事务创建或复用 `prepared` ExternalOperation → 使用由 effect identity 派生的正式 idempotency key 调用实际 authority → 保存 acknowledged response → 查询实际业务状态 → `confirmed|failed|indeterminate`。模型、Hermes、Bridge 和 API 调用方不得提供或覆盖正式幂等键；Execution、Attempt、Worker、进程和 retry 变化只能继续同一 operation。

### 整改完成判据

- 每个实际业务副作用均能从真实业务对象反向追踪到唯一 Task Contract、effect intent、CanonicalEffectRequest、effect identity、Approval（如需要）和 ExternalOperation。
- 同一 `authority_domain + effect_identity` 在 Execution、Attempt、Worker、进程和重启变化后仍只存在一个 ExternalOperation 和一个对应实际业务效果。
- 模型提供的 idempotency key 不能覆盖正式 key；改变 tool call ID、Execution ID、nonce 或调用顺序不能获得新授权。
- approval mismatch、stale Contract、未授权 effect、未知 normalizer 和 cancelled/terminal Task 必须在实际外部调用前拒绝。
- 无法取得 authoritative confirmation 时必须保持 `indeterminate` 并进入 reconciliation，不能用 Tool Result、Receipt 或 Agent 自述判定成功。
- 测试若直接调用 `prepare_external_operation`、预制 confirmed、手工写 Approval/Receipt 或使用 fake/mock 业务结果，整改判据无效。

## S-04：恢复 cancel 权威事实与迟到写保护

### 原设计

> cancel_task(command_id)  
> → Task version CAS  
> → 持久化 cancel intent  
> → Task cancelled  
> → 当前 Attempt cancelled  
> → 未开始 Run Request cancelled  
> → pending Interaction cancelled  
> → best-effort AgentExecutor.cancel()

> Runtime 必须先提交取消事务，再通知进程内 Executor。重复 cancel 必须幂等。

> Task 取消后：Gateway 拒绝新的副作用；迟到结果只能审计，不能覆盖 `cancelled`；外部状态不确定时进入 reconciliation。

### 原设计来源

- 冻结点：`phase4-p4.0-contract-frozen`
- `docs/phase4/TASK_RUNTIME.md`，blob `424ae6bada383d4b318406f436afc4f5c6a7b83f`，第 10 节“Cancel”及 10.1“cancel 与 completion 竞争”，冻结原文第 329—363 行。
- `docs/phase3/APPROVAL_BINDING.md`，blob `7e5c2e1577c50893ca4e8ace6455677f8f13fc9f`，第 6 节 Approval match 条件，冻结原文第 161—174 行，其中明确要求 `Task/Attempt not cancelled or terminal`。

### 当前 Production 偏差

`TaskRuntime` 没有完整的 `cancel_task` 外部命令。Gateway 只检查当前 legacy Execution 的 `suspension_requested`，不检查 Task/Attempt 的取消或终态。`prepare_external_operation` 只加载 Contract，没有在同一事务内验证 Task、Attempt、Execution 或当前 Worker ownership，因此 cancelled Task 仍可准备或执行新的副作用，迟到结果也缺少统一保护。

### 对应业务代码位置

- `astra/runtime.py:99-577`：`TaskRuntime` 现有命令中缺少 `cancel_task`。
- `astra/tool_gateway.py:112-124`：只调用 `is_suspended(execution_id)`。
- `astra/phase3/governance.py:960-969`：prepare 只读取 Task Contract，没有读取 Task/Attempt 当前 state/version。
- `astra/runtime.py:500-538`：result 持久化没有 cancel/terminal/lease 条件保护。

### 之前为什么错

此前容易把 cancel 保护描述为新增建议或仅补一个 `cancel_task` 方法；冻结设计要求的是持久化权威 cancel intent、同事务生命周期变化、Gateway 新副作用拒绝、迟到结果保护和未知外部状态对账的完整 Production 链。

### 正确整改方法

实现冻结设计规定的幂等 `cancel_task(command_id)`：在一个权威事务中校验 command identity 和 Task version CAS，持久化 cancel intent，将 Task/Attempt 置为 cancelled，取消未开始 Run Request 和 pending Interaction，再进行 best-effort Executor cancel。Gateway、ExternalOperation prepare/dispatch、Execution result、Policy apply 和 finalize 在写入时必须重新校验 Task/Attempt/Execution 当前状态、version 和有效 ownership；迟到记录只能审计或触发 reconciliation，不得改变终态。

### 整改完成判据

- 通过正式 cancel 入口提交后，cancel intent、Task、Attempt、未开始 Run Request 和 pending Interaction 在同一事务形成冻结设计规定的状态，重启后仍有效。
- 相同 command_id/payload 重放返回同一语义结果且不重复派生记录；相同 ID 不同 payload 返回 identity conflict。
- cancel 成功后，真实旧 Worker、旧 Execution 和新 Tool 请求均不能创建、dispatch 或 confirm 新副作用。
- cancel/complete 竞争只由首个成功的 Task version CAS 决定；terminal 不被迟到结果覆盖，不使用时间戳裁决。
- 已可能 dispatch 的操作必须进入 reconciliation；无法确认不得伪装为已撤销或已成功。
- 任何未授权副作用、terminal 覆盖、半事务取消或永久 running 且无工作项均立即判定整改失败；测试补写 cancelled 状态或直接调用内部 cancel 不构成完成证据。

## S-05：恢复 waiting_input 与 waiting_approval 的统一 Runtime 生命周期

### 原设计

> Hermes 或 Policy 请求输入  
> → Runtime 持久化 Interaction  
> → 当前 Execution 终结  
> → Task/Attempt 进入等待  
> → resolve_interaction  
> → 同事务解决 Interaction、推进 Task/Attempt、创建 Run Request  
> → 同一 Attempt 创建新 Execution

> CanonicalEffectRequest  
> → 精确匹配 ApprovalRequirement  
> → ApprovalRequest + Interaction  
> → Task waiting_approval  
> → ApprovalResolution  
> → 新 Run Request / 新 Execution  
> → Gateway 验证 exact approval binding

> 无论请求来源是谁，只有 Task Runtime 可以持久化 Interaction，并在同一事务修改 Task/Attempt 状态、记录 Fact 和可选 Outbox。

### 原设计来源

- 冻结点：`phase4-p4.0-contract-frozen`
- `docs/phase4/TASK_RUNTIME.md`，blob `424ae6bada383d4b318406f436afc4f5c6a7b83f`，第 9 节“waiting_input 与 waiting_approval”，冻结原文第 286—327 行。
- 冻结点：`phase3-governance-complete`
- `docs/phase3/TASK_LIFECYCLE.md`，blob `2599e2f20906b5957c68e20fb8d9a8d2c53e339d`，第 5 节“Interaction ownership”，冻结原文第 106—118 行。
- `docs/phase3/APPROVAL_BINDING.md`，blob `7e5c2e1577c50893ca4e8ace6455677f8f13fc9f`，第 8 节“Interaction lifecycle”，冻结原文第 205—221 行。

### 当前 Production 偏差

Hermes Adapter 直接通过 legacy Store 创建 `interactions` 并设置 Execution suspension；Governance 只读取和创建 `phase3_interactions`。waiting_input、waiting_approval、ApprovalRequest/Resolution 和 Worker Run Request 因此不在同一权威事务链。`Phase2Runtime.resolve_and_resume` 只修改 legacy Interaction 并返回内存中的新 Invocation，不持久化新的 Run Request 或 Execution。审批拒绝分支还可能结束当前 Execution/Run Request，却留下 running Task 且没有 Interaction 或下一工作项。

### 对应业务代码位置

- `astra/hermes_adapter/context.py:105-120`：Adapter 直接调用 legacy `store.request_interaction`。
- `astra/storage.py:470-535`：legacy Interaction 创建和 resolve。
- `astra/phase3/governance.py:813-829`：Governance 只读取 `phase3_interactions`。
- `astra/phase3/governance.py:1486-1510`：Governance 创建另一套 Interaction。
- `astra/runtime.py:739-775`：resolve 后只返回内存 Invocation，没有持久化 Run Request/Execution。
- `astra/runtime.py:668-682`：Governance blocked 时结束 Execution/Run Request 后直接抛错。

### 之前为什么错

此前把问题简化为“统一两张 Interaction 表”，但冻结设计的核心不是表同步，而是 Interaction 只能由 Task Runtime 持久化，并且等待、当前 Execution 终结、Task/Attempt 状态、resolve 和新 Run Request 必须形成同一可恢复生命周期。同步两张表仍然是双权威和补丁式修复。

### 正确整改方法

Adapter、Gateway 和 Policy 只能向 Task Runtime 提交 Interaction 请求，不得直接持久化。Runtime 使用唯一 Interaction 权威记录，在同一事务中创建 pending Interaction、终结当前 Execution、完成当前 Run Request、推进 Task/Attempt 到 waiting，并写必要 Fact/Outbox。`resolve_interaction(command_id, expected_version)` 在同一事务保存 Resolution、推进 Task/Attempt、创建唯一新 Run Request；Worker 随后在同一 Attempt 创建新 Execution。主动审批与 Gateway 强制审批必须汇合到同一 ApprovalRequest/Interaction 路径并验证 exact effect identity；拒绝或缺失审批必须进入冻结设计规定的 waiting、failed 或 reconciliation 路径，不能留下永久悬挂。

### 整改完成判据

- Hermes 主动输入、Hermes 主动审批、Gateway 强制审批和 Policy 请求均由同一 Task Runtime Production 路径创建唯一 Interaction。
- Interaction pending、Execution 终结、Run Request 完成、Task/Attempt waiting 和 Fact/Outbox 在权威事务中保持一致，进程可安全退出并从数据库恢复。
- 正式 resolve 命令幂等解决 Interaction，并在同一事务创建唯一新 Run Request；新 Worker Execution 位于同一 Attempt 且读取持久化 Resolution feedback，不恢复旧 Python 栈。
- exact Approval 的 Contract、requirement、effect identity/request hash、permission scope、validity 和 usage 任一不匹配均被拒绝，且实际业务副作用为零。
- cancel/resolve 竞争只由事务和 CAS 决定，不产生 terminal Task 的可执行 Run Request。
- 任何双表分歧、resolved 无 Run Request、running 无工作项或测试手工创建 Interaction/Approval 的成功均判定整改失败。

## S-06：恢复 PolicyAction 到持久化 Runtime 工作项的原子映射

### 原设计

> `continue_with_feedback`：同一 Attempt 创建新 Execution，沿用 Hermes Session。

> `start_new_attempt`：原子结束旧 Attempt 并创建新 Attempt，默认使用新 Hermes Session。

> `request_input` / `request_approval`：请求 Runtime 创建 Interaction。

> `reconcile`：请求只读 Evidence Collection/对账，不执行补偿副作用。

> Runtime 在应用 Decision 的同一事务创建 Interaction、修改 Task/Attempt 状态、写 Fact/Outbox。

> 允许的触发原因包括：
>
> ```text
> task_submitted
> interaction_resolved
> continue_with_feedback
> new_attempt
> restart_recovery
> reconciliation_completed
> ```

### 原设计来源

- 冻结点：`phase3-governance-complete`
- `docs/phase3/TASK_POLICY.md`，blob `098d61cb9b13a78ac102ef86079cf25d9544415e`，第 2 节“Allowed actions”、第 5 节“Bounds”、第 6 节“Interaction responsibility”、第 7 节“Normal decision mapping”，冻结原文第 21—45、97—155 行。
- 冻结点：`phase4-p4.0-contract-frozen`
- `docs/phase4/TASK_RUNTIME.md`，blob `424ae6bada383d4b318406f436afc4f5c6a7b83f`，第 7 节“Run Request 与首版调度”和第 8 节“单 Worker Execution 闭环”，冻结原文第 230—284 行。

### 当前 Production 偏差

Governance 应用 `continue_with_feedback` 时创建 `phase3_executions`，但没有创建 Worker 消费的 `phase4_run_requests`；`start_new_attempt` 创建新 Attempt 后同样没有创建 Run Request。Interaction 和 reconciliation 也只是 Governance 派生记录，没有证明对应 Runtime worker/resolver 会继续处理。Limits 在 Contract 中存在，但没有贯穿所有 action 应用点形成实际上限闭环。

### 对应业务代码位置

- `astra/phase3/governance.py:1371-1484`：PolicyAction 状态应用。
- `astra/phase3/governance.py:1409-1412`：continue 只创建 `phase3_execution`。
- `astra/phase3/governance.py:1413-1457`：new attempt 只创建 Attempt。
- `astra/phase3/governance.py:1486-1532`：只创建 Governance Interaction/Reconciliation 记录。
- `astra/runtime.py:243-365`：Worker 只 claim `phase4_run_requests`。

### 之前为什么错

此前只写“补齐映射”，没有明确冻结设计要求的是 PolicyDecision 应用与实际可消费 Runtime 工作项之间的同一事务、持久化、exactly-once 闭环。仅创建领域对象或状态行不能证明任务能够继续执行。

### 正确整改方法

在 Runtime 应用 PolicyDecision 的权威事务中，按冻结 action 语义创建唯一派生工作：`continue_with_feedback` 在同一 Attempt 保存 feedback 并创建 Run Request；`start_new_attempt` 原子终结旧 Attempt、创建新 Attempt 和 Run Request；`request_input/request_approval` 创建唯一 Interaction；`reconcile` 创建只读 reconciliation 工作；`complete/fail/escalate` 通过 Completion Gate、CAS 和终态规则应用。所有派生对象使用 `created_by_decision_id` 或等价唯一约束；在应用前读取权威 limits/counters，超过上限只能 fail 或 escalate。

### 整改完成判据

- 每个正式 PolicyDecision 由 Production 应用一次且只派生一个与 action 对应的持久化工作项，重启重放不重复创建。
- `continue_with_feedback` 保持 Attempt identity 并产生 Worker 可 claim 的新 Run Request；`start_new_attempt` 原子结束旧 Attempt、创建 ordinal 单调的新 Attempt 和 Run Request。
- `request_input/request_approval` 只由 Runtime 创建 Interaction；`reconcile` 只有只读 Evidence/对账能力，不获得业务写接口。
- max attempts、execution、feedback、reconcile 和 deadline 上限在所有相关创建点强制执行，超过上限后不再创建新工作。
- Task 最终只能进入冻结设计允许的终态或可恢复状态，不存在 decision 已 applied 但无 Run Request/Interaction/reconciliation 的永久悬挂。
- 测试直接调用 Policy、写 Decision、创建派生对象或手工推进 Task 的结果不得作为完成证据。

## S-07：按 CompletionRequirement 与权威证据恢复 Evaluator 隔离判定

### 原设计

> Core 将 `configuration` 视为 evaluator-owned opaque mapping，不发展通用表达式语言。Completion Contract 必须固定 evaluator version，防止插件升级改变旧 Task 的验收语义。

> 正式流程：
>
> ```text
> Evidence Collector
> → persist authoritative observations
> → immutable EvidenceSnapshot
> → pure Requirement Evaluator
> ```

> 对副作用 Requirement，EvidenceSnapshot 应携带 Task Contract ref、effect identity/request hash、ExternalOperation 和必要 Approval refs。

> 第一版聚合优先级：
>
> ```text
> 任何 required evaluator_error → evaluator_error
> 否则任何 required unsatisfied → unsatisfied
> 否则任何 required unknown → indeterminate
> 全部 required satisfied → satisfied
> ```

> Aggregator 不理解 Complaint、Ticket、Customer、Order 等业务字段。

### 原设计来源

- 冻结点：`phase3-governance-complete`
- `docs/phase3/REQUIREMENT_EVALUATORS.md`，blob `afa828ead36e66e9b16dd1b0cf6a2b28bf0ea88b`，第 3 节“CompletionRequirement”、第 4 节“Evidence collection before evaluation”、第 5 节“Evaluator Registry”、第 6 节“RequirementEvaluation”、第 7 节“Completion Aggregator”和第 8 节“Responsibility separation”，冻结原文第 34—92、111—160 行。

### 当前 Production 偏差

`AuthorizedEffectConfirmedEvaluator` 不读取具体 Requirement 的 configuration、required evidence、subject、effect intent 或业务 observation；它只要在同一 Task Contract 下找到任意 confirmed ExternalOperation 就返回 `satisfied`。因此一个 operation 可以错误满足多个独立 required requirements，并产生 false success。Production 默认 registry 为空时，真实业务又会得到 evaluator_error，而测试通过手工注册掩盖该问题。

### 对应业务代码位置

- `astra/phase3/completion.py:214-265`：Evaluator 的 matching 只检查 Contract ref 和 effect hash，忽略 Requirement configuration 和具体业务 observation。
- `astra/phase3/completion.py:126-150`：Aggregator 按状态聚合，但无法修复单项 Evaluator 的错误满足。
- `astra/phase3/governance.py:690-699`：Production 使用 registry 对所有 requirements 求值并聚合。

### 之前为什么错

此前提出统一绑定 subject、effect intent、authority domain、参数和 freshness 的通用方案，容易把新的通用表达式塞进 Core；冻结设计明确规定 configuration 由具体 Evaluator 拥有，Aggregator 不理解业务字段。正确修复必须落在版本化业务 Evaluator，而不是扩展通用 Aggregator 语义。

### 正确整改方法

为每个冻结 evaluator ID/version 实现对应业务插件，严格读取该 Requirement 的 evaluator-owned configuration 和 required evidence，只使用固定 EvidenceSnapshot、Receipt snapshots、ExternalOperation、Approval refs 和 authoritative business observations 判定该单项 Requirement。Complaint Evaluator 必须验证设计示例规定的正确 customer/order/ticket 关系；其他 evaluator 按各自冻结配置判断。Aggregator 保持业务无关，只执行原有优先级；缺证据保持 `unknown/indeterminate`，插件或配置错误保持 `evaluator_error`，不得默认成功。

### 整改完成判据

- 每个 required Requirement 都有与其 evaluator ID/version 和 configuration 一致的独立 RequirementEvaluation 和证据引用。
- 只完成其中一个业务 effect 时，其他独立 Requirement 仍为 unsatisfied/unknown；一个无关 confirmed operation 不得自动满足多个 Requirement。
- Receipt、Tool Result、Agent 自述或 Operation `confirmed` 单独不能替代 required authoritative business observation。
- `unknown` 聚合为 indeterminate，`evaluator_error` 聚合为 evaluator_error；二者均不能使 Task succeeded。
- 只有全部 required requirements satisfied 且 Runtime completion invariants 通过时，Production 才能通过 CAS 将 Task 置为 succeeded。
- 测试手工注册 evaluator、预制 operation/observation、直接写 confirmed 或读取 Production CompletionValidationResult 作为业务真值时，完成判据无效。

## S-08：按权威版本恢复 EvidenceSnapshot identity 与确定性重放

### 原设计

> 一次 DecisionContext 必须绑定固定版本：
>
> - Task row/version；
> - Task Contract ID/version/hash；
> - Attempt row/version；
> - 相关 Execution terminal records；
> - Receipt IDs 和 content hashes；
> - Interaction IDs、versions 和状态；
> - ExternalOperation IDs、versions 和状态；
> - effect identity/request hash 和 Approval Request/Resolution versions（如适用）；
> - EvidenceSnapshot ID；
> - Fact watermark；
> - CompletionValidation ID（如果存在）。

> ```text
> EvidenceSnapshot:
> (task_id, collection_trigger_id, authoritative_versions_hash, collector_version)
> ```

> 相同 collection trigger 和相同权威版本应复用同一个 Snapshot，而不是重复产生内容相同的新 Snapshot。

> Evaluator、Rule 和 Policy 不得在一次决策中混用 Snapshot 之外的后到状态。

### 原设计来源

- 冻结点：`phase3-governance-complete`
- `docs/phase3/DECISION_POINTS_AND_SNAPSHOTS.md`，blob `44f08b89368f47d54319177c8d9410562040f2c0`，第 2 节“Snapshot construction”、第 4 节“Evaluation idempotency”和第 8 节“Failure and restart behavior”，冻结原文第 20—36、66—87、139—145 行。
- `docs/phase3/RELIABILITY_FACTS_AND_EVIDENCE.md`，blob `2fc9ab102066b93697b088d8901ffa59e27210c9`，第 8 节“EvidenceSnapshot”，冻结原文第 200—223 行。

### 当前 Production 偏差

Snapshot 内容虽保存了部分 Interaction、ExternalOperation、Approval 和 Fact 数据，但 `evidence_snapshot_id` 只由 `execution_id`、`result_hash`、`task_version` 和 `attempt_version` 生成，没有纳入 ExternalOperation/Interaction/Approval/Receipt/Observation/Facts 的权威版本。operation 从 prepared 变为 confirmed 后可能继续命中旧 Snapshot identity，导致 Evaluation identity conflict 或重放读取不同事实。

### 对应业务代码位置

- `astra/phase3/governance.py:660-689`：Snapshot ID 仅使用 execution/result/task/attempt 四项，内容随后才附加 operations、interactions、approvals 和 watermark。
- `astra/phase3/completion.py:17-58`：Snapshot content hash 包含模型内容，但外部传入的 snapshot ID 与完整 authoritative versions 未绑定。
- `astra/phase3/completion.py:165-185`：RequirementEvaluation identity 依赖 `evidence_snapshot_id`，放大错误 identity 的影响。

### 之前为什么错

此前只说“给 Snapshot 增加字段”，没有区分 Snapshot 内容完整与 Snapshot identity 正确是两个不同义务。即使内容包含 operation，identity 不包含其 version/status，仍无法满足冻结设计的 authoritative_versions_hash 和确定性复用语义。

### 正确整改方法

在一个数据库一致性读取事务中固定所有本地权威版本；先由 Evidence Collection 将外部状态固定为带 identity/version/hash 的 Observation，再按冻结唯一键生成或复用 Snapshot：`task_id + collection_trigger_id + authoritative_versions_hash + collector_version`。`authoritative_versions_hash` 必须覆盖设计列出的 Task/Contract/Attempt/Execution/Receipt/Interaction/ExternalOperation/effect/Approval/Observation/Fact watermark/Completion 引用。Evaluator、Rule 和 Policy 只读取该 Snapshot；权威版本变化产生新 Snapshot，相同 trigger 和版本复用旧 Snapshot。

### 整改完成判据

- 相同 collection trigger、完全相同权威版本和 collector version 在重启前后返回同一 Snapshot identity，不重复创建语义相同记录。
- ExternalOperation status/version、Interaction/Approval version、Receipt hash、business observation、Fact watermark 或 Task/Attempt/Contract version 任一影响裁决的变化都会产生新 Snapshot identity。
- 固定 Snapshot 重放得到相同 RequirementEvaluation、RuleEvaluation 和 PolicyDecision，不读取 Snapshot 外后到状态，也不产生 identity conflict。
- Policy 求值后/apply 前崩溃能够复用同一 DecisionContext/Decision；CAS stale 时拒绝应用。
- 测试直接修改 Snapshot、权威记录或 Evaluation identity 得到的结果无效；无法从 Production 导出完整版本链时不得判定完成。

## S-09：恢复冻结版本组件的 Production 注册与错误语义

### 原设计

> Registry key：
>
> ```text
> (evaluator_id, evaluator_version)
> ```

> 第一版使用静态注册。未注册或异常 Evaluator 返回明确 `evaluator_error`，不得默认通过。

> Rule 必须：
>
> - 使用稳定 `rule_id` 和显式 `rule_version`；
> - 相同 DecisionContext 和 Snapshot 产生相同输出。
>
> 第一版使用静态 Python registry 或固定配置映射，不建设规则 DSL、动态策略语言或远程插件平台。新增 Rule 不应要求修改中央 Detector switch。

> Normalizer 必须：
>
> - 固定 `normalizer_id/version`；
> - 对未知或歧义语义 fail closed。

> unknown normalizer/version fail closed。

### 原设计来源

- 冻结点：`phase3-governance-complete`
- `docs/phase3/REQUIREMENT_EVALUATORS.md`，blob `afa828ead36e66e9b16dd1b0cf6a2b28bf0ea88b`，第 5 节“Evaluator Registry”和第 10 节“Contract tests”，冻结原文第 84—92、166—175 行。
- `docs/phase3/TASK_RULES.md`，blob `aad365c14881413ac0597dc9052df52e27d9008c`，第 4 节“Purity and registration”和第 7 节“Contract tests”，冻结原文第 81—92、130—138 行。
- `docs/phase3/EFFECT_IDENTITY_AND_IDEMPOTENCY.md`，blob `d3eb1bbb3d464241b6a357394902991786d04a0a`，第 3 节“Normalization requirements”和第 12 节“Contract tests”，冻结原文第 88—108、293—304 行。

### 当前 Production 偏差

`TaskRuntime` 默认创建 `RuntimeGovernanceCore`，而 Core 默认创建空 evaluator registry。标准 Evaluator 主要由 tests 或 Round 2 辅助路径手工注册，Production 没有证明正式启动时会装配 Contract 引用的 Evaluator、Normalizer、Constraint Handler 和 Rule。真实 Task 因此可能得到 `unknown_evaluator → evaluator_error → escalate`，而测试环境通过手工注册得到成功。

### 对应业务代码位置

- `astra/runtime.py:99-105`：TaskRuntime 直接构造 Governance Core。
- `astra/phase3/governance.py:352-362`：未传 registry 时创建空 `RequirementEvaluatorRegistry()`。
- `astra/phase3/completion.py:143-201`：未知或异常 evaluator 返回 `evaluator_error`。
- `tests/test_phase4_persistent_runtime.py:1113-1117`、`1206-1209`：测试手工注册 Evaluator 后再执行。

### 之前为什么错

此前一度提出“启动时预扫描全部 Contract 并引入 blocked 状态”。冻结设计只规定静态注册、版本 identity 和未知组件的 fail-closed/evaluator_error 语义，没有规定新的 `blocked` 状态、启动扫描范围或恢复协议。该部分属于原设计缺口，不能作为整改实现。

### 正确整改方法

只恢复冻结设计：Production 正式组装必须静态注册发布版本支持的 Evaluator、Normalizer、Constraint Handler 和 Task Rule，并保持其稳定 ID/version；Contract 引用的版本必须由 Production 自己解析。unknown evaluator 保持 `evaluator_error`，unknown normalizer/constraint 保持 fail closed，缺证据保持 `unknown/indeterminate`。禁止测试在运行前后补注册。启动预扫描、`blocked` 状态或新的兼容协议标记为 `DESIGN GAP`，本整改不得实现。

### 整改完成判据

- 仅使用正式 Production 启动方式，所有受支持 Contract 引用的 evaluator/normalizer/constraint/rule 均已注册并可在真实任务链中使用，无测试注入。
- 相同 ID/version 在重启和重放后保持相同语义；旧 Task 不被新版本静默重新解释。
- unknown evaluator 必须产生 `evaluator_error`，unknown normalizer/constraint 必须在业务动作前 fail closed，且均不得进入 Task success。
- Evaluator/Rule 保持纯函数职责，不修改 Task、不调用 Tool、不创建 Interaction/Reconciliation。
- 任何成功依赖测试手工注册、fixture registry、monkeypatch 或直接替换 Production 组件时，整改无效。
- 启动预扫描等未冻结能力保持 `DESIGN GAP`，不得因其未实现而伪报本项完成，也不得擅自实现。

## S-10：恢复 Run Request 执行资格、budget/deadline 与异常收敛闭环

### 原设计

> - terminal/cancelled Task 的 Run Request 不可执行；
> - claim、完成和取消均为事务性状态转换。

> 多进程协调前必须先完成：
>
> ```text
> submit Task
> → 同事务创建 Task、初始 Attempt 和 Run Request
> → 单 Worker claim
> → 创建 Execution
> → 构造 RuntimeInvocation
> → HermesExecutor 执行
> → 持久化 ExecutionResult
> → Completion / Governance 求值
> → Task 终态或下一 Run Request
> ```

> Execution Driver 必须在执行前重新检查 Task/Attempt 状态、budget 和 deadline。

> deadline 语义固定为失败，不是取消：
>
> ```text
> Task deadline exceeded
> → Task failed
> → termination_reason = task_deadline_exceeded
>
> Attempt deadline exceeded
> → Attempt exhausted
> → termination_reason = attempt_deadline_exceeded
> ```

> 失去执行者的 running Execution 必须终结为：
>
> ```text
> state = interrupted
> termination_reason = process_lost
> ```

### 原设计来源

- 冻结点：`phase4-p4.0-contract-frozen`
- `docs/phase4/TASK_RUNTIME.md`，blob `424ae6bada383d4b318406f436afc4f5c6a7b83f`，第 7 节“Run Request 与首版调度”、第 8 节“单 Worker Execution 闭环”、第 12 节“Restart recovery”和第 13 节“Budget 与 deadline”，冻结原文第 230—284、394—432、451—485 行。

### 当前 Production 偏差

claim 查询先按排序选择首个 pending Run Request，之后才检查 Task/Attempt 是否可执行；遇到终态或失效请求会直接抛错，队首毒请求可阻塞后续有效任务。deadline 更新 Task/Attempt 后没有同步使关联 Run Request 不可执行。`SingleWorker.run_once` 对 `executor.execute` 没有统一异常保护；异常会遗留 running Execution、claimed Run Request 和 running Task，且当前没有 startup recovery 收敛这些记录。

### 对应业务代码位置

- `astra/runtime.py:259-300`：先选择 pending request，再检查 Task/Attempt eligibility。
- `astra/runtime.py:302-323`：deadline 到期只调用 Governance expire，当前 Run Request 未同步终结。
- `astra/runtime.py:637-711`：`executor.execute` 异常没有统一终结/恢复保护。
- `astra/runtime.py:550-576`：只允许 claimed→completed，没有异常/失效请求分类。

### 之前为什么错

此前只关注“超限 Task 是否被标记 failed”或“Executor 是否抛出异常”，没有按照冻结设计验证整个队列和生命周期是否继续可用。把毒化 pending request 保留为测试预期，会用当前缺陷反向定义语义。

### 正确整改方法

claim 只能对当前具有执行资格的 Task/Attempt/Run Request 成功；终态、cancelled、非当前 Attempt、deadline/budget 已耗尽的请求必须通过权威事务形成冻结设计允许的不可执行状态，不得反复阻塞队列。deadline/budget 检查覆盖设计列出的 Attempt、Run Request、Execution start、Interaction resolution、restart recovery 和 reconciliation 完成点。Worker 在 claim、Execution、Executor、result、Governance 和 finalize 的正式链路上统一收敛异常：本进程可处理错误形成明确 Execution terminal/result；进程丢失由 Production startup recovery 终结为 `interrupted/process_lost` 并创建设计允许的下一工作。

### 整改完成判据

- 终态/cancelled/非当前 Attempt/超限 Task 的 Run Request 不会被执行，也不会持续阻塞其后的有效请求。
- Task deadline、Attempt deadline 和 budget 在冻结设计要求的所有边界生效，并使用稳定 termination reason；deadline 不被误写为 cancelled。
- Executor 正常返回、真实异常、cancel、timeout 和进程丢失后，Execution、Run Request、Task/Attempt 均形成可解释的权威状态，不存在永久 running/claimed 残留。
- 失去执行者的 running Execution 由 Production recovery 确定性终结为 `interrupted/process_lost`，需要继续时创建新 Run Request/Execution，不复用旧 Execution。
- 一个毒请求不得造成后续有效任务饥饿；停止故障后所有具有执行资格的工作可继续推进。
- fake executor 抛预设异常、测试直接改 pending/claimed、手工 finalize 或手工清队列均不能作为整改完成证据。

## S-11：恢复 Phase 2 Adapter 正式执行边界的持久化终结

### 原设计

> Astra × Hermes 第一条可运行、可验证、可持久化终结的投诉处理纵向闭环已经通过真实 Hermes Plugin discovery、Tool Registry、 `AIAgent` Loop、Tool execution、Observer Hooks 和 SessionDB 验证。

> Trace 从 `ExecutionStarted` 开始，以 `ExecutionEnded` 结束。

> ```text
> Astra RuntimeInvocation
> → HermesExecutor
> → Hermes Agent Loop
> → Astra Bridge Plugin
> → Astra Tool Gateway
> → Business Adapter / external system
> → submit_task_result
> → Astra Completion Validation / Policy / Runtime transition
> ```

> Adapter 外层 `try/finally` 只能保证本进程尽力收敛，不能单独提供 exactly-once。Phase 2 必须同时使用内存状态机和数据库原子更新或唯一约束。

> 只有赢得持久化竞争的调用方可以生成正式终结记录、发布唯一 `AstraExecutionEnded` 并推进 Runtime。正常返回、异常、cancel、timeout、Hook/Adapter 重复结束、重启补偿和重复消费都必须经过同一约束。

### 原设计来源

- P2 权威基线：`docs/PHASE2_CORE_BASELINE.md`，blob `03767fb12ce459939d467a217919661d55a3efe7`，第 1 节“Baseline 结论”和第 3 节“Trace、Receipt 和持久化审计”，冻结原文第 6—8、41—90 行。
- P2 架构权威：`docs/HERMES_ARCHITECTURE_ANALYSIS.md`，blob `9eef601653e014f0b0d05ee48c9b590c9d4d61c4`，第 10.5 节“运行模型”和第 10.6 节“Exactly-once Execution 终结”，冻结原文第 671—700 行。
- 冻结点：`phase3-governance-complete`
- `docs/adr/ADR-003-task-reliability-boundary.md`，blob `d1d23f1ac85605770c10d648aafe9e0e278c0aa9`，“业务执行边界”，冻结原文第 44—85 行。

### 当前 Production 偏差

`HermesExecutor.execute` 在没有确保权威 Execution 已存在时立即发出 `ExecutionStarted` Trace；Trace 表对 legacy `executions` 有外键，因此直接正式调用可在首次 Trace 写入时报外键错误。Executor 返回后也不执行持久化 exactly-once finalize。现有测试通过调用前手工 `start_execution`、调用后手工 `finalize_execution` 补偿，掩盖正式入口不能独立形成持久化终结闭环。

### 对应业务代码位置

- `astra/hermes_adapter/executor.py:47-91`：先 emit `ExecutionStarted/Ended`，不建立或持久化终结 Execution。
- `astra/trace.py` 与 `astra/storage.py:244-262`：Trace span 外键引用 legacy `executions`。
- `tests/test_phase2_hermes_e2e.py:369-398`：测试手工 start/finalize。
- `tests/test_phase2_live_provider.py:117-130`：live test 同样手工 start/finalize。

### 之前为什么错

此前一度允许“恢复入口或移除入口二选一”，这会用当前回归实现反向解释冻结设计。冻结设计要求正式 Runtime→Adapter 业务边界具有可持久化、唯一的 Execution 终结；测试侧手工初始化和终结不属于 Production 行为。

### 正确整改方法

恢复现有正式 Runtime→HermesExecutor 边界的完整 Production 语义：在任何 Execution Trace 或 Hermes 调用前，由正式 Runtime/Adapter 边界确保权威 Execution 已创建；正常返回、异常、cancel、timeout、重复结束和恢复补偿全部通过同一数据库 exactly-once 条件更新或唯一约束发布唯一 `AstraExecutionEnded` 并推进 Runtime。不得依赖调用方测试手工 start/finalize。若要取消既有公开入口身份，属于设计变更，本整改不得自行实施。

### 整改完成判据

- 仅通过正式 Production 入口调用实际 HermesExecutor，不预建 Execution、不手工 finalize，也能形成从 `ExecutionStarted` 到唯一 `AstraExecutionEnded` 的完整持久化记录。
- 正常返回、实际异常、cancel、timeout、重复 finalize 和重启补偿均经过同一 exactly-once 约束；每个 Execution 最多一个正式终结事件。
- Trace、Receipt、ResultReceipt 和 Execution 外键完整，首次 Trace 不发生外键失败。
- ExecutionResult 的 agent turn、task outcome 和最终 Task state 保持冻结设计规定的职责分离，不以 Hermes `completed` 直接完成 Task。
- 任何成功仍依赖测试 `start_execution/finalize_execution`、fake Provider 或直接写数据库时，整改无效。
- 若正式入口和责任归属无法按冻结设计验证，状态只能为 `BLOCKED`，不得宣称完成。

## S-12：恢复 Checkpoint、restart recovery 与 reconciliation Production 闭环

### 原设计

> Checkpoint 是安全生命周期边界上的持久化恢复信封，不是 Python 调用栈、Hermes 内部执行快照、token 级快照、Tool 步骤列表或 Workflow 游标。

> 恢复时权威顺序固定为：
>
> ```text
> 数据库权威记录 > Checkpoint > Hermes session context
> ```

> Runtime 启动时至少扫描：
>
> - 已失去有效执行权的 running Execution；
> - 已解决但缺少 Run Request 的 Interaction；
> - running Task 缺少可执行工作；
> - pending PolicyDecision 和 pending Outbox；
> - 未完成 reconciliation；
> - 未达到确定终态的 ExternalOperation。

> 失去执行者的 running Execution 必须终结为：
>
> ```text
> state = interrupted
> termination_reason = process_lost
> ```

> 原 Execution 不得继续使用。恢复必须读取持久化事实和 Checkpoint，并创建新的 Run Request 与新的 Execution；不得恢复旧 Python 调用栈。

> 无法证明操作尚未 dispatch 时，Runtime 必须保守进入 reconciliation。

### 原设计来源

- 冻结点：`phase4-p4.0-contract-frozen`
- `docs/phase4/TASK_RUNTIME.md`，blob `424ae6bada383d4b318406f436afc4f5c6a7b83f`，第 11 节“Checkpoint”、第 12 节“Restart recovery”、12.1“恢复分类”和 12.2“prepared ExternalOperation”，冻结原文第 365—449 行。
- `docs/adr/ADR-004-persistent-task-runtime.md`，blob `9a057a323e94ecb38114f58872d8f8aa9085c24a`，Decision 3“Restart recovery 创建新 Execution”和 Decision 4“Checkpoint 是持久化恢复信封”，冻结原文第 46—66 行。

### 当前 Production 偏差

当前 schema 没有 checkpoint 权威表或引用，Runtime 没有 startup scanner、`recover_execution` 生产命令、orphan classifier 或 recovery Run Request 生成器。Governance 只能创建 pending reconciliation 记录，没有实际 runner 查询业务权威状态并推进 operation/Task。进程崩溃后 running Execution、claimed Run Request 和未知外部操作无法按冻结分类自动收敛。

### 对应业务代码位置

- `astra/storage.py:156-333`：现有 Runtime schema 没有 checkpoint、lease 或 recovery command 数据结构。
- `astra/runtime.py:99-577`：没有 `recover_execution` 和 startup scan。
- `astra/phase3/governance.py:1513-1532`：只创建 pending reconciliation 记录。
- `astra/phase3/governance.py:1274-1310`：只读取 unresolved reconciliation 状态，没有执行 runner。

### 之前为什么错

此前容易把“增加 crash 测试”或“存在 reconciliation 表”当作恢复能力。冻结设计要求的是 Production 在重启时主动扫描、分类、终结 orphan、创建新工作并对实际外部状态进行对账；测试不能代替 scanner、recover command 或 reconciliation runner。

### 正确整改方法

按冻结设计实现持久化 Checkpoint，只保存最小权威引用/version/hash；实现 Runtime startup scanner，识别设计列出的所有异常状态；实现带 command identity/CAS 的 `recover_execution`，终结 orphaned Execution 为 `interrupted/process_lost` 并按分类创建新 Run Request/Execution或保持终态；实现 reconciliation runner，通过实际业务系统只读查询更新 ExternalOperation、EvidenceSnapshot、Completion 和 Policy。`prepared + confirmed_not_dispatched` 才可用同一 identity 继续；dispatch unknown、in_flight、acknowledged、indeterminate 全部先对账。

### 整改完成判据

- 在冻结设计列出的每个安全边界真实强杀 Worker 并重启后，Production startup scanner 能仅凭数据库权威记录和 Checkpoint 自动分类并恢复，无人工写库。
- orphaned running Execution 唯一终结为 `interrupted/process_lost`；继续任务时创建新 Run Request 和新 Execution，旧 Execution 不再使用，Python 栈不恢复。
- resolved Interaction 缺 Run Request、running Task 缺工作、pending Decision/Outbox、reconciliation 和未确定 operation 均被扫描并形成设计规定的下一状态。
- 可能已 dispatch 的真实外部操作先查询实际业务状态；最终最多一个业务效果，无法确认保持 `indeterminate`。
- cancelled/terminal/deadline/budget exhausted Task 不创建普通恢复 Execution。
- 测试直接调用 recover、预写 checkpoint/operation 状态、伪造远端响应或用异常替代真实进程强杀时，不构成完成证据。

## S-13：恢复 lease、heartbeat、takeover 与 fencing

### 原设计

> 多进程切片中，Worker claim Run Request 时原子写入 owner、不可复用的 lease token 和 expiry。同一时刻只有一个 token 有效。

> 在创建 Execution、开始调用、续租、保存 terminal result 和 finalize Run Request 前验证当前 token。

> Lease 到期后其他进程可以 takeover，并获得新 token。旧 Worker 恢复运行后，其 heartbeat、result 和 finalize 写入必须被 fencing 拒绝。

> - lease 表示 Worker 对 Run Request 的临时执行权；
> - heartbeat 为长 Execution 续租；
> - 每次 claim/takeover 生成新的 fencing token；
> - 旧 token 不能 heartbeat、提交结果或 finalize；

### 原设计来源

- 冻结点：`phase4-p4.0-contract-frozen`
- `docs/phase4/TASK_RUNTIME.md`，blob `424ae6bada383d4b318406f436afc4f5c6a7b83f`，第 14 节“Lease、heartbeat 与 fencing”，冻结原文第 487—504 行。
- `docs/adr/ADR-004-persistent-task-runtime.md`，blob `9a057a323e94ecb38114f58872d8f8aa9085c24a`，Decision 5“同一主机多进程通过 lease、fencing 和 CAS 协调”，冻结原文第 68—79 行。

### 当前 Production 偏差

Run Request schema 只有 state、priority、ready_at 和创建信息，没有 owner、lease token、expiry、heartbeat 或 ownership version。claim 只把 `pending` 更新为 `claimed`；Execution 创建、Tool dispatch、result 保存和 finalize 都不携带或校验 token。发生进程停顿和 takeover 时，旧 Worker 仍可继续写入或产生副作用。

### 对应业务代码位置

- `astra/storage.py:274-289`：`phase4_run_requests` 缺少 owner/token/expiry/heartbeat。
- `astra/runtime.py:336-363`：claim 仅执行 `state='claimed'` 并创建 Execution。
- `astra/runtime.py:500-538`：保存 result 不校验 lease token。
- `astra/runtime.py:550-576`：完成 Run Request 不校验 owner/token。
- `astra/tool_gateway.py:77-135`：Tool 调用上下文没有 lease/fencing 校验。

### 之前为什么错

此前把普通 SQLite 事务 claim 描述为“部分具备单一 claim”，容易把原子 state 更新误当成 lease。冻结设计要求可过期 ownership、每次 claim/takeover 的不可复用 token，以及旧 Worker 在所有关键写入点被 fencing；普通 `pending→claimed` 不满足这些语义。

### 正确整改方法

在现有 Run Request 权威模型中加入冻结设计要求的 owner、不可复用 lease token、expiry 和 heartbeat/version；claim/takeover 在同一数据库事务中生成新 token。创建 Execution、开始 Hermes 调用、heartbeat、ExternalOperation prepare/dispatch、保存 terminal result、Governance apply 和 finalize 前均验证当前 token；takeover 后旧 token 的任何状态写和新副作用通过条件更新/CAS 被拒绝。保持首版同一主机、共享本地 SQLite 的部署边界，不引入跨主机协调或新平台。

### 整改完成判据

- 两个或更多独立 Production Worker 共享同一本地 SQLite 竞争同一 Run Request 时，同一时刻只有一个有效 lease token 和一个实际执行者。
- active lease 未到期时第二 Worker 不能提前 takeover；到期后 takeover 生成不可复用的新 token。
- 旧 Worker 恢复后，其 heartbeat、Tool side effect、result、Governance apply 和 finalize 全部因 stale token 被拒绝，不能覆盖新 Worker 状态。
- 最终只存在一个有效 Execution 结果和符合 effect identity 唯一约束的实际业务效果。
- lease 不替代 Task/Attempt CAS、PolicyDecision 幂等、Completion Gate 或 ExternalOperation 唯一约束。
- 单进程 happy path、进程内锁、伪造 token、直接写库或 fake Worker 竞争不能证明整改完成。

## S-14：恢复 Reliability Fact、Outbox、Dispatcher 与 Task Rule 运行闭环

### 原设计

> 对于 Astra 控制的同一数据库，必须在一个事务内提交：
>
> ```text
> authoritative record mutation
> + reliability_fact
> + optional outbox row
> ```

> Fact 已持久化与 Fact 已投递必须分离。

> Task Rule 可以检测：
>
> ```text
> cross_attempt_no_progress
> duplicate_side_effect
> late_state_affecting_record
> recovery_divergence
> receipt_business_state_mismatch
> ```

> Rule 必须：
>
> - 确定性、无副作用、可重放；
> - 不修改 Task、Attempt、Execution、Receipt、Interaction 或 Fact；
> - 不触发 Tool、Interaction、reconciliation 或新的 Attempt；

> Fact/Outbox 投递失败：权威记录与 Fact 已原子提交，Dispatcher 幂等重投。

### 原设计来源

- 冻结点：`phase3-governance-complete`
- `docs/phase3/RELIABILITY_FACTS_AND_EVIDENCE.md`，blob `2fc9ab102066b93697b088d8901ffa59e27210c9`，第 5 节“Local atomic publication”、第 6 节“External operation ledger”和第 9 节“Neutral Trace separation”，冻结原文第 103—176、225—229 行。
- `docs/phase3/TASK_RULES.md`，blob `aad365c14881413ac0597dc9052df52e27d9008c`，第 1 节“Responsibility”、第 4 节“Purity and registration”、第 5 节“Rule semantics”和第 6 节“Explicit non-overlap”，冻结原文第 6—29、81—128 行。
- `docs/phase3/DECISION_POINTS_AND_SNAPSHOTS.md`，blob `44f08b89368f47d54319177c8d9410562040f2c0`，第 8 节“Failure and restart behavior”，冻结原文第 139—145 行。

### 当前 Production 偏差

Governance 能在同一事务插入 Reliability Fact 和 Outbox，但没有 Production Dispatcher、delivery retry、startup scan 或持续 Rule runner。真实 Gateway 副作用绕过 Canonical Effect/Governance，因此 operation/receipt/business observation 也没有进入完整 Fact 链。现有 Rule 主要是领域定义，没有从 Runtime 固定 Snapshot/Fact window 触发并将 finding 交给 Policy 的生产闭环。

### 对应业务代码位置

- `astra/phase3/governance.py:1624-1669`：只完成 Fact/Outbox 原子插入。
- `astra/phase3/governance.py:310-325`：Outbox 表存在，但仓库无正式 Dispatcher 消费实现。
- `astra/phase3/governance.py:1513-1532`：reconciliation 只有 pending 记录。
- `astra/tool_gateway.py:112-135`：真实副作用写 legacy Receipt，不发布对应 ExternalOperation/Facts。
- `astra/phase3/facts.py`、`astra/phase3/policy.py`：存在模型/输入，但没有持续 Production runner。

### 之前为什么错

此前看到表、模型和原子插入函数后，容易把该能力评价为“部分完成”。冻结设计要求权威状态原子发布、Outbox 投递分离、Dispatcher 幂等重投、固定 Snapshot/Fact window 的 Rule 求值以及 Policy 后续动作；只有表和写函数不能证明运行闭环。

### 正确整改方法

将所有影响 Task truth 的正式事务按冻结设计原子写权威记录、Reliability Fact 和可选 Outbox；实现 Production Dispatcher 读取 pending Outbox、记录 delivery attempts 并幂等重投，Runtime 启动时恢复未投递记录。实现固定 Rule runner，仅消费 DecisionContext、EvidenceSnapshot 和截至 fact watermark 的 supporting facts，输出设计定义的 evaluation/finding/indeterminate，再交由 Policy 决定动作。真实 Gateway/ExternalOperation/Receipt/Business Observation 必须进入该权威事实链；Rule 不得获得状态写或 Tool 接口。

### 整改完成判据

- 每个影响 Task truth 的正式状态变化都能证明权威记录、Fact 和必要 Outbox 在同一事务完整提交或完整回滚。
- 权威提交后、Outbox 投递前真实进程中断，重启后 Production Dispatcher 能幂等重投；重复投递不重复改变业务状态。
- 设计列出的五类 Task Rule 均由 Production runner 基于固定 Snapshot/Fact window 求值，相同输入重放结果相同。
- Rule finding 只作为 Policy 输入，不直接写 Task、创建 Interaction/Attempt/Reconciliation 或调用 Tool。
- Gateway 的真实副作用、ExternalOperation confirmation、Receipt 和 business observation 均有可追踪 Fact/Outbox 证据。
- 测试手工插入 Fact/Outbox、直接调用 Rule、伪造 delivery 或预制 finding 得到的成功无效。

## S-15：独立 Evaluation Oracle 与后续验收要求的设计冲突

### 原设计

> **Neutral Trace Collector 与 Evaluation Oracle 位于被测系统控制逻辑之外**。Neutral Trace 对 A/B/C 三组使用相同 Observer 契约，只记录、不干预；Evaluation Oracle 使用统一 Rubric、Neutral Trace 和 Mock 业务真值独立评分，不依赖 Agent 自述、C 组 CompletionValidationResult 或 PolicyDecision。

> 必须实现独立于 A/B/C 被测系统的 Evaluation Oracle。它不是 Astra Reliability Harness 的运行模块，不参与 Agent 决策、不改变 Task 状态、不触发 Task Policy，也不把 C 组 CompletionValidationResult 或 PolicyDecision 直接当作实验真值。

> Oracle 只生成 `EvaluationResult`，不得修改 Task、TaskAttempt 或任何生产运行状态。

> 每项验收必须由自动化测试证明，不以手工演示代替。

> Phase 3 frozen contract、governance、pytest 和 ruff Gate 原样通过；live Provider smoke 继续是 environment-dependent non-blocking test。

### 原设计来源

- P2 架构权威：`docs/development_updated.md`，blob `8d07ffd6a76d97c494a16b8464d254bbb99de311`，系统边界中 Evaluation Oracle 说明及第八部分“独立 Evaluation Oracle”，冻结原文第 225—235、898—949 行。
- 冻结点：`phase4-p4.0-contract-frozen`
- `docs/phase4/ACCEPTANCE_MATRIX.md`，blob `fb4feba07e3e9a37e6b9163b6278290394c1764c`，第 1 节“使用规则”、第 2 节“顶层验收”、第 4 节“Crash-point matrix”、第 5 节“Multi-process matrix”、第 6 节“Phase 3 regression Gate”和第 7 节“Milestone Gate”，冻结原文第 7—13、40、96—172 行。

### 当前 Production 偏差

现有 tests 手工 start/finalize Execution、手工注册 evaluator、使用 `DeterministicFakeExecutor`，并在 fake executor 内直接写 ApprovalResolution、prepare ExternalOperation、转为 confirmed；部分 deadline/approval 测试把 Production 悬挂或毒化状态写成预期。测试因此参与了被测系统生命周期和业务真值构造，违反原设计中 Oracle 只观察、不改变状态的边界。README/PHASE_STATUS 的完成声明也高于 Production 实际闭环。

同时存在必须明确报告的设计冲突：冻结 Phase 4 文档把 pytest/fixture Gate 和 live Provider non-blocking 写入验收；后续 Testing Philosophy 禁止以 pytest、fixture、mock/fake 或缺少真实 Provider 的结果证明完成。后续要求不得冒充原设计，因此本冲突不能由整改文档自行消解。

### 对应业务代码位置

- `tests/test_phase2_hermes_e2e.py:369-398`、`tests/test_phase2_live_provider.py:117-130`：测试手工 start/finalize Execution。
- `tests/test_phase4_persistent_runtime.py:1113-1177`：手工注册 Evaluator，fake executor 内直接记录 Approval、prepare/confirm ExternalOperation。
- `tests/test_phase4_persistent_runtime.py:1206-1235`：手工 registry + fake executor 驱动审批缺失路径。
- `astra/runtime.py:579-620`：Production 包内存在 `DeterministicFakeExecutor`。
- `README.md`、`docs/PHASE_STATUS.md`：完成度声明与实际 Production 闭环不一致。

### 之前为什么错

此前把 pytest/fixture/fake executor 产生的绿色结果当成 Production 语义证据，并让测试补齐 Production 未完成的 Execution、Approval、ExternalOperation 和 evaluator 组装。后来又把 Testing Philosophy 和真实 Provider 要求当作冻结原设计，忽略原 P4 文档明确保留 pytest Gate、Mock Business State 和 non-blocking live Provider；两种做法都混淆了原设计、后续要求和当前测试。

### 正确整改方法

`DESIGN CONFLICT / BLOCKED`。冲突双方分别是：冻结设计要求独立 Evaluation Oracle，同时保留自动化测试、pytest/ruff Gate、Mock Business State 和 environment-dependent non-blocking live Provider；后续验收要求禁止以 pytest、fixture、mock/fake 或缺少真实 Provider 的结果证明完成。两者对验收权威和环境要求的规定不一致，当前无法确定应执行哪一种设计。该冲突必须由设计责任方裁决；裁决前停止本项整改，不提供默认处理方案。

### 整改完成判据

- 当前不存在可执行的整改完成判据。
- 设计责任方未完成冲突裁决前，本项状态必须保持 `BLOCKED`，不得修改测试预期、选择任一默认方案或判定完成。
- 无法证明适用设计已经明确时，最终结论只能是 `BLOCKED`。

## S-16：稳定性量化要求的原设计缺口

### 原设计

`DESIGN GAP`：审计前冻结的 Phase 3/Phase 4 原始设计规定了部署边界、确定性状态不变量、crash window、并发竞态、CAS、lease/fencing 和恢复分类，但没有规定统一的运行时长、任务规模、故障比例、恢复时限、队列排空时限、资源上限或其他稳定性量化验收标准。

原设计仅明确：

> Phase 4 第一版只承诺：
>
> ```text
> 同一主机
> 多个独立 Runtime Worker 进程
> 共享一个本地 SQLite 数据库
> 使用数据库事务、CAS、lease 和 fencing 协调
> ```

> 首版不承诺跨主机协调，不支持把 SQLite 放在无法保证其锁和一致性语义的网络文件系统上，也不引入 Kafka、Redis、Celery 或其他分布式协调设施。

> - 高级调度公平性、aging、复杂 deadline 排序；
> - Worker 管理平台；

### 原设计来源

- 冻结点：`phase4-p4.0-contract-frozen`
- `docs/phase4/TASK_RUNTIME.md`，blob `424ae6bada383d4b318406f436afc4f5c6a7b83f`，第 3 节“首版部署与并发边界”、第 16 节“非目标”和第 17 节“变更纪律”，冻结原文第 82—94、557—576 行。
- 冻结 Phase 3/Phase 4 权威文档中没有上述稳定性量化验收标准。因此不存在可引用的原设计完成标准。

### 当前 Production 偏差

这不是 Production 未实现某个冻结量化目标的问题，而是原设计没有定义可执行的稳定性量化验收标准。当前 Production 同时还缺失 recovery、lease/fencing、真实副作用治理等确定性能力，因此也不能用任意自选压力次数或运行时长推导生产稳定性结论。

### 对应业务代码位置

- 无单一业务代码位置：该项属于原设计缺口，不得通过修改某个函数或增加测试常量解决。
- 相关确定性能力缺失分别位于 `astra/runtime.py`、`astra/storage.py`、`astra/tool_gateway.py` 和 `astra/phase3/governance.py`，应由 S-01—S-14 按冻结设计整改。

### 之前为什么错

此前自行写入“30 次、100 次、2 小时、1000 个 Task”等阈值，并指定了原设计不存在的额外设计形式。这些内容都不是审计前冻结设计原文，属于测试或整改文档替设计补充需求，违反“原设计优先”和“Design Gap 必须停止报告”。

### 正确整改方法

冻结设计缺少稳定性量化验收标准，因此生产稳定性目前不可裁决。需设计方补充正式设计后，再开展对应整改和验收。

### 整改完成判据

- 当前不存在可由冻结设计导出的整改完成判据。
- 设计方未补充正式设计前，本项状态必须保持 `DESIGN GAP`，不得判定完成。
- 无法证明稳定性量化验收标准已经由设计方明确时，最终结论只能是 `DESIGN GAP`。

# 第三部分：测试验收方案

## 3.0 Testing Philosophy（最高测试原则）

以下原则是本方案所有测试设计、执行、证据采集和验收裁决的最高约束。任何测试方法、工具或历史惯例与其冲突时，以本节为准：

1. **测试验证设计，不验证当前实现。** 测试必须从需求、ADR 和冻结工程设计推导预期，用于判断 Production 是否实现了设计；不得根据当前代码能够做到什么来降低或重写预期。
2. **Production 是唯一权威。** 任何由测试补偿、测试构造、内部注入、直接写库、替身组件或绕过正式入口得到的成功均视为无效。只有正式生产组装和正式业务链产生的状态与结果可以作为验收对象。
3. **发现问题时修 Production，而不是修测试。** 当测试暴露 Production 与设计不一致时，应修改生产代码、生产配置或生产迁移；只有需求或权威设计经过正式变更后，才允许同步更新测试预期。
4. **测试只能驱动系统运行，不得代替系统完成生命周期、业务逻辑或状态转换。** 测试可以提交命令、提供真实交互输入、启动或强杀进程、查询权威状态，但不得替系统创建/终结 Execution、解析 Policy、生成 Approval、确认 ExternalOperation、注册生产组件或推进 Task 状态。
5. **验收依据来自需求与设计，不来自现有代码、历史测试或通过率。** 当前实现、历史 fixture、既有测试断言、pytest/CI 结果和覆盖率只能提供线索，不能定义冻结语义，也不能成为通过依据。
6. **测试的价值在于暴露问题，而不是制造“全部通过”的假象。** 对失败、悬挂、不可观察、环境缺失和设计漂移必须如实报告；不得通过删减场景、弱化断言、改写预期、使用 happy path 或把无法验证记为通过来获得绿色结果。

## 3.1 总体裁决规则

### 3.1.1 需求与设计导出门禁

每个验收场景在实现前必须先形成 `requirement-to-test` 记录，并且只能按以下顺序推导：

```text
需求/Authority Manifest
→ ADR 与冻结 Contract/Runtime 文档
→ 可观察的不变量和状态转换
→ Production 公开输入
→ 独立 Oracle 的观察方法
→ 测试驱动步骤
```

记录必须包含设计章节、前置状态、允许的公开输入、期望的权威状态转换、期望的实际业务结果、禁止发生的结果和允许采集的证据。当前代码分支、现有测试断言、fixture 字段和历史通过结果不得出现在“期望来源”中，只能作为发现实现缺陷的辅助材料。

若需求与冻结设计没有定义某项预期，或不同权威文档相互冲突，该场景必须标记为“设计阻断（非通过）”，先完成设计澄清、正式变更和 Authority Manifest 更新。测试不得选择当前代码最容易满足的解释，也不得自行增加状态、动作、阈值或异常语义。

### 3.1.2 Production 路径与测试驱动边界

Production 指发布时实际使用的 composition root、Runtime、Worker、Hermes Adapter、Gateway、Governance、registries、数据库迁移、业务 Adapter、Dispatcher 和 Recovery 组件组成的完整运行链。只有该运行链可以创建、转换和终结 Task 生命周期及业务副作用。

测试驱动器只允许执行用户或运维在 Production 中也能执行的动作：启动/停止服务、通过公开 CLI/API 提交 Contract 和 Task、提供 Interaction/Approval resolution、发送 cancel、制造真实外部故障、强杀独立进程，以及通过公开查询或只读审计接口采集证据。

测试驱动器不得直接调用内部 Governance/Store 方法完成状态转换，不得直接插入或修改 Task、Attempt、Execution、Interaction、Approval、ExternalOperation、Receipt、Snapshot、Fact、Outbox 或 Run Request，不得替 Production 注册组件、生成正式 identity、确认业务操作或补做终结。若某项生命周期只能靠测试执行这些动作才能完成，裁决必须是 Production 缺陷。

### 3.1.3 独立 Oracle 与唯一允许的验收证据

Oracle 只负责观察并比较“设计要求”和“Production 实际结果”，不得参与被测系统决策。每个验收项必须同时具备：

1. **设计来源证据**：需求、ADR、冻结 Contract/Runtime 文档的精确章节和由其推导的不变量；
2. **真实入口证据**：正式 CLI/API/Worker/composition root 接收了哪些公开输入，不直接调用内部治理函数拼装结果；
3. **权威状态证据**：从实际运行数据库或正式审计接口只读导出的 identity、version、state 和关联；
4. **实际业务结果证据**：从正式业务 Adapter 背后的实际 sandbox/测试租户业务系统查询对象和状态，并回链 subject、effect identity、受保护的 idempotency reference 和 external operation ID；
5. **独立裁决证据**：Oracle 根据设计不变量比较业务真值与 Task truth，不读取 Agent 自述、CompletionValidationResult、PolicyDecision 或测试预期作为真值。

缺少任意一类证据即不能通过。日志、Trace、Agent 最终回答、Tool Result、测试断言文本和人工解释不能单独证明业务完成。

### 3.1.4 明确禁止的测试与修复方式

- 禁止以 `pytest passed`、测试数量、coverage、ruff、Gate 或 CI 绿色作为正确性证明；
- 禁止根据现有 tests/fixtures、当前数据库 schema 或当前代码路径解释、冻结或降低产品语义；
- 禁止测试专用的 fake executor、mock/stub business service、monkeypatch、内存替身、录制 Provider response、预制 Approval/ExternalOperation/Receipt/Observation；
- 禁止固定写死成功结果、identity、confirmed 状态和只覆盖 happy path 的样本；
- 禁止验收驱动器直接写数据库构造前置状态、竞争结果或恢复结果；
- 禁止用测试内抛异常代替进程崩溃，用伪造返回代替真实网络/Provider/业务故障；
- 禁止为使测试转绿而删除失败场景、弱化断言、接受永久悬挂或把 Production 缺失逻辑搬入测试；
- 禁止因当前实现无法满足设计而修改测试预期。只有完成正式设计变更后，才能重新推导并更新测试。

如果当前正式配置只能使用 `MockBusinessService`、fake Provider、测试专用 registry 或测试侧初始化补偿，则相关能力裁决为 Production/环境阻断，不得以这些替代物产生的绿色结果验收。

### 3.1.5 数据、故障与环境要求

- 使用与待发布版本相同的 Production composition root、schema migration、配置和公开命令；
- Hermes 使用实际集成版本和真实 Provider；缺少凭证时记录“环境阻断（非通过）”，不以录制响应或假客户端代替；
- 业务系统使用隔离的真实 sandbox/测试租户，通过正式 API 获取、创建和查询业务对象；测试只提供公开业务输入，不预写最终状态；
- 旧 schema 数据必须来自正式历史版本运行产生的数据库或经审计的发布快照，不能由当前测试按目标结果手工拼装；
- 并发使用设计规定的独立进程和共享 SQLite 部署边界；
- crash 通过外部观察公开/权威里程碑后真实强杀进程，不在被测代码中增加只供测试使用的状态推进逻辑；
- 故障必须发生在真实依赖边界，例如停止 Worker、撤销网络、使用 Provider 的真实错误响应或业务 sandbox 的正式拒绝能力。测试不得伪造 Production 已完成的恢复分类。

### 3.1.6 裁决状态与稳定性标准

验收只允许以下裁决：

- **通过**：设计语义明确，Production 独立完成完整链路，全部权威证据与业务真值一致；
- **失败**：Production 行为违反设计或业务真值；
- **设计阻断（非通过）**：需求/设计缺失、冲突或不可裁决；
- **环境阻断（非通过）**：真实 Provider、业务系统、凭证或部署边界不可用。

确定性安全不变量采用零容忍：未经授权副作用、重复 confirmed 外部操作、终态覆盖、stale lease 写入、错误 completion 和半事务状态只要出现一次即失败。

冻结设计没有规定重复次数、持续时间、工作负载规模、故障比例、恢复时限和资源上限，因此生产稳定性目前不可裁决。需设计方补充正式设计后，再开展对应整改和验收；测试不得自行规定数字或据此判定通过。

### 3.1.7 六项 Testing Philosophy 的全文执行检查

| 原则 | 本文中的强制执行方式 | 发现违反时的裁决 |
|---|---|---|
| 测试验证设计 | 每个 BV 条目必须先有设计依据；3.1.1 强制 requirement-to-test 推导；无设计依据的稳定性数字已删除。 | 设计阻断或测试设计无效，不执行“适配当前代码”的验收。 |
| Production 唯一权威 | 3.1.2 限制测试只能使用公开入口；BV 条目明确禁止预写记录、直接调内部 Gateway/Policy/Store、测试补终结和测试注册组件。 | 由测试补偿产生的所有成功证据作废，记录 Production 缺陷。 |
| 问题修 Production | P-16、S-15 和 3.1.4 禁止为转绿改写测试；S-11 要求按现有设计修复 Executor，除非先正式变更设计。 | 恢复原设计预期，提交 Production 整改；测试不得降低断言。 |
| 测试只驱动运行 | 每个 BV 的验收方式限定为 Contract/Task/resolve/cancel 等公开命令、真实依赖故障和 OS 进程控制；状态转换全部由 Production 完成。 | 直接写库、直接调用内部状态转换或伪造故障的场景无效。 |
| 验收来自需求与设计 | 每项列明设计章节；3.1.3 要求独立 Oracle 比较设计与业务真值；BV-20 要求可机读 traceability。 | 引用当前代码、fixture、pytest 或历史通过率的结论无效。 |
| 测试用于暴露问题 | 所有 BV 均有明确失败条件；3.1.6 保留失败、设计阻断和环境阻断，不允许折算成通过；BV-19 禁止 happy path-only。 | 原样报告缺陷或阻断并阻止发布，不得删场景或修改预期制造绿色。 |

## 3.2 验收条目

### BV-01：正式生产组装与入口自洽

**设计依据**：ADR-003；ADR-004 第 1、6 节；`TASK_RUNTIME.md` 第 2、8 节。

**验收方式**：从干净环境仅使用发布文档声明的正式命令启动 Store、Runtime、Worker、Hermes Adapter、Gateway、registries、Dispatcher 和 Recovery Scanner；不导入 tests，不执行 fixture 初始化，不手工 start/finalize Execution。通过正式入口提交一个真实业务 Task，并观察完整闭环。

**通过标准**：启动时所有 Contract 引用组件均可解析；Task 自动形成 Attempt、Run Request、Execution；Trace 外键有效；执行结果进入 Governance；最终进入设计规定的终态或可恢复等待态。按现有 Phase 2 设计公开的 HermesExecutor 入口必须独立自洽；测试不得通过预建 Execution 或补做 finalize 使其成功。

**失败条件**：需要测试代码补注册、补记录、补终结，出现外键错误，或同一发布包存在两套不同生产组装。

### BV-02：生命周期持久化与单一权威

**设计依据**：`TASK_LIFECYCLE.md`；`TASK_RUNTIME.md` 第 4、5 节；P4-A01 的设计目标。

**验收方式**：分别在 running、waiting_input、waiting_approval、reconciling 和终态时关闭数据库并重启全新进程，使用公开查询接口和数据库只读导出核对 identity/version/state/Contract refs。执行旧 schema 原地升级，并继续处理升级前未完成 Task。

**通过标准**：所有对象跨重启完整保留；每类对象只有一个权威写模型；不存在 legacy/phase3 双写分歧；迁移失败时 Worker 不 claim；成功迁移可重复且不丢历史记录。

**失败条件**：同一业务事件在两套表状态不一致，重启后需要人工修库，或旧数据只能删除重建。

### BV-03：命令幂等与 CAS

**设计依据**：`TASK_RUNTIME.md` 第 6 节；`DECISION_POINTS_AND_SNAPSHOTS.md` 第 4—6 节。

**验收方式**：对 submit、resolve、cancel、record result、recover 使用同一 command_id 重放，并在设计规定的两个或更多独立进程中并发提交；随后用相同 command_id 和不同 payload 冲突提交，重启后再次重放。本项只裁决冻结设计已经规定的确定性不变量，不自行增加重复规模作为完成标准。

**通过标准**：相同 identity 返回首次语义结果且不新增 Task/Attempt/Execution/Interaction/Run Request/Decision/Operation；不同 payload 稳定返回 identity conflict；CAS loser 不产生部分派生记录。

**失败条件**：依赖内存缓存保证幂等、重启后重复创建对象、以时间戳决定竞争胜者或冲突被静默覆盖。

### BV-04：Task Contract、工具和约束强制

**设计依据**：`TASK_CONTRACT.md` 第 5—10、13 节；ADR-003 Constraint Boundary。

**验收方式**：仅通过正式 Contract 发布/Task 提交入口提供设计允许的输入，驱动以下场景：允许工具、未允许工具、错误 capability、工具版本漂移、schema hash 漂移、未知 constraint/version、subject/scope 越界、参数缺失/多余/类型错误。所有 Tool 请求必须由真实 Hermes→Bridge→Gateway 路径产生；测试不得直接调用 Gateway 或改写持久化 Contract。若 Production 没有公开 Contract 发布/提交入口，该能力直接判为 Production 不完整。

**通过标准**：仅完全匹配冻结 Contract 的调用进入业务 Adapter；所有不匹配调用在副作用前被确定性拒绝，并记录结构化原因和 Contract identity；未知项 fail closed；实际执行 schema 与 Contract hash 可回链。

**失败条件**：只检查工具名或硬编码模型、依赖 Prompt 限制、业务系统已收到越权请求，或调用方可覆盖 Contract 版本。

### BV-05：Canonical Effect、正式幂等和 ExternalOperation 唯一性

**设计依据**：`EFFECT_IDENTITY_AND_IDEMPOTENCY.md`；`RELIABILITY_FACTS_AND_EVIDENCE.md` 第 6 节；P4-A12 设计目标。

**验收方式**：通过正式 Task 入口让 Production 对同一真实业务 effect 在同一/不同 Execution、Attempt、Worker、进程和重启后自然重试；通过公开业务输入提供设计定义为语义等价和语义不同的参数。测试不得直接调用 normalizer、`prepare_external_operation` 或向数据库预写 operation；Oracle 从实际业务系统查询最终对象数。本项不自行增加冻结设计未规定的并发规模。

**通过标准**：模型提供的幂等键被忽略或拒绝；正式 identity/key 由 Normalizer 和 Contract 派生；相同 effect 只有一个 ExternalOperation 和一个实际业务对象；不同 effect 产生不同 identity；operation 可从 prepared 追踪到 authoritative confirmation。

**失败条件**：只靠 receipt 唯一索引、存在多个 confirmed 远端对象、改变 tool_call_id/Execution 即可重复执行，或无法从实际业务对象回链 effect identity。

### BV-06：审批精确绑定与可恢复等待

**设计依据**：`APPROVAL_BINDING.md` 第 3—10 节；`TASK_RUNTIME.md` 第 9.2 节；P4-A04 设计目标。

**验收方式**：通过正式 Task/Approval/resolve 入口和真实业务输入分别产生无审批、正确审批、effect mismatch、subject mismatch、参数变化、Contract version 变化、过期审批、重复消费，以及主动审批和 Gateway 强制审批场景。ApprovalRequest、Interaction 和 Resolution 必须由 Production 自己创建和推进。等待后停止全部进程，再由正式 resolve 命令恢复。

**通过标准**：无有效审批时业务系统无副作用且 Task 进入可查询的 `waiting_approval`；ApprovalRequest、Interaction、Resolution 和 ExternalOperation 绑定同一 effect identity；正确 resolution 原子创建新 Run Request；stale/mismatch 被拒绝且 Task 仍处于明确可处理状态。

**失败条件**：宽泛文本批准即可执行、测试直接插入 ApprovalResolution、拒绝后 Task running 且无工作项，或审批恢复继续旧 Python 调用栈。

### BV-07：waiting_input 跨重启恢复

**设计依据**：`TASK_LIFECYCLE.md` 第 5 节；`TASK_RUNTIME.md` 第 9.1、9.3 节；P4-A03 设计目标。

**验收方式**：真实 Hermes 执行中请求必要输入；确认等待状态后强杀进程并重启；通过公开 resolve 命令提交动态输入，同时并发发起 cancel 竞争。

**通过标准**：Interaction、Execution 终结、Run Request 完成、Task/Attempt 等待在同一持久化闭环中；resolve 原子地产生新 Run Request，并在同一 Attempt 创建新 Execution；新执行收到准确 resolution feedback；cancel/resolve 严格遵守事务胜者规则。

**失败条件**：Interaction 写入非权威表、Task 仍 running、恢复旧调用栈、resolved 后无 Run Request，或 terminal Task 仍有可执行请求。

### BV-08：Policy continue、新 Attempt 和治理上限

**设计依据**：`TASK_POLICY.md` 第 2、5、7 节；`TASK_CONTRACT.md` 第 9 节；`TASK_RUNTIME.md` 第 4.2、8、13 节。

**验收方式**：通过正式业务输入和真实外部结果产生设计规定的证据不足、可纠正失败、跨 Attempt 无进展和 reconciliation 场景，由 Production 自己求值并选择 PolicyAction；limits 只能通过正式 Task Contract 输入设置，并跨重启继续。测试不得直接调用 Policy、写入 finding/decision 或创建派生工作项。

**通过标准**：每个 decision 恰好派生一个正确工作项；continue 保持 Attempt 不变，新 Attempt ordinal 单调且仅在授权条件出现；达到上限后不再创建工作项，并产生冻结的 termination reason；所有计数跨重启保持。

**失败条件**：只写 phase3_execution 而无 Run Request、默认策略永不可能产生设计动作、次数上限只在测试断言中存在，或无限循环。

### BV-09：取消、迟到结果和迟到副作用保护

**设计依据**：`TASK_RUNTIME.md` 第 10 节；P4-A05/P4-A06 设计目标。

**验收方式**：通过公开 cancel 命令在 pending、claimed、执行中、等待中以及设计定义的外部 dispatch 边界前后取消；并发执行 cancel/complete。取消后保留或恢复真实旧 Worker 进程，使其按原执行自然尝试后续 Tool、result、finalize；测试不得直接调用这些内部写接口。重启后重复公开 cancel 命令。

**通过标准**：cancel intent 跨重启有效且幂等；未开始请求和 pending Interaction 被取消；取消后的新副作用为 0；迟到结果不覆盖终态；dispatch 状态不明的操作进入 reconciliation；cancel/complete 由首次成功 CAS 决定。

**失败条件**：cancelled Task 仍可 prepared/dispatch operation、以时间戳判胜者、迟到 Worker 可写终态，或取消只调用内存 interrupt。

### BV-10：Completion Requirement 隔离和真实业务完成

**设计依据**：`REQUIREMENT_EVALUATORS.md` 第 3—8 节；`DECISION_POINTS_AND_SNAPSHOTS.md` 第 7 节；`EFFECT_IDENTITY_AND_IDEMPOTENCY.md` 第 9 节。

**验收方式**：通过正式 Contract/Task 入口提交包含两个或更多独立 required effects/observations 的真实 Task，利用实际业务输入分别形成只完成其中一项、错误 subject、参数不匹配、只有 Tool Result 而无权威业务对象、以及全部正确完成等设计已定义的状态。Production 必须自行产生 operation、observation 和 evaluation；Oracle 直接查询实际业务系统，不读取 CompletionValidationResult 作为真值。

**通过标准**：每个 Requirement 只被完全匹配的 operation + authoritative observation 满足；一项完成不能自动满足其他项；缺证据为 unknown/indeterminate；evaluator error 不得 success；仅当全部 required requirements 和 runtime invariants 满足时 Task 才 succeeded。

**失败条件**：任意 confirmed operation 可满足所有 requirement、Receipt/Agent 文本代替业务状态，或存在 false success。

### BV-11：EvidenceSnapshot 确定性与权威变化重放

**设计依据**：`RELIABILITY_FACTS_AND_EVIDENCE.md` 第 7—9 节；`DECISION_POINTS_AND_SNAPSHOTS.md` 第 2、4、8 节。

**验收方式**：先让 Production 完整产生一个 Snapshot，再通过正式业务操作、Interaction/Approval resolution、reconciliation 和 Runtime 命令推动 ExternalOperation、Observation、Fact watermark 与 Task version 按设计自然变化，随后由 Production 重新求值并跨重启重放。测试不得直接修改 Snapshot 或任何被引用权威记录，也不得自行增加冻结设计未规定的重复规模。

**通过标准**：相同 Snapshot 得到相同 evaluation/decision identity 且不重复写派生记录；任何影响裁决的权威变化产生新 Snapshot；历史 Snapshot 仍可重放；不存在 identity conflict 或读取 Snapshot 外可变状态。

**失败条件**：operation 从 prepared 到 confirmed 后仍复用旧 identity、重放结果受当前数据库状态影响，或缺少关键权威版本。

### BV-12：Evaluator/Normalizer/Constraint/Rule 生产注册

**设计依据**：`REQUIREMENT_EVALUATORS.md` 第 5 节；`TASK_RULES.md` 第 4 节；Task Contract 版本规则。

**验收方式**：仅通过正式启动入口加载所有组件，通过正式 Contract/Task 入口提交每个支持版本、未知版本和旧历史版本引用，随后重启并升级 Production。测试不得直接读取或修改 registry，也不得在启动后补注册。若项目要验收“启动前预扫描”，必须先完成 S-09 所要求的正式 Runtime 设计变更。

**通过标准**：支持版本无需测试注入即可运行；旧版本可按原版本重放；未知 normalizer/constraint fail closed，未知 evaluator 按冻结设计形成 `evaluator_error` 和对应 Policy 结果；注册表内容与发布 manifest 和冻结设计一致。只有正式设计已经冻结启动预扫描时，才额外验收启动阶段行为。

**失败条件**：tests 手工注册后才通过、运行中随机 unknown evaluator、升级后用新版本解释历史记录。

### BV-13：deadline/budget 与队列持续可用性

**设计依据**：`TASK_RUNTIME.md` 第 7、13 节；P4-A13 设计目标。

**验收方式**：通过正式 Contract/Task 入口提交具有设计规定 deadline/budget 的任务，并依靠实际时钟、真实 Provider usage 和 Production 计数自然达到边界；随后通过正式入口提交仍有效的任务，跨重启观察 claim 和队列排空。测试不得直接修改 deadline、budget ledger、Run Request 排序键或 Task 状态。

**通过标准**：超限任务使用稳定 termination reason 进入正确终态；关联 pending/claimed 请求同步取消或恢复分类；调度器跳过不可执行项，后续有效任务全部被处理；不存在同一毒请求被无限重复 claim。

**失败条件**：只证明超限 Task failed 而未证明队列健康，或任何终态 Task 保留可执行 pending 请求。

### BV-14：Executor 异常与 exactly-once 终结

**设计依据**：Phase 2 Adapter 终结约束；`TASK_RUNTIME.md` 第 8、12 节。

**验收方式**：使用实际 Hermes/Provider/业务链制造真实认证失败、网络中断、Provider 超时、业务 Adapter 错误和进程信号；不得用 fake executor 抛预设异常。跨重启检查每种设计定义的错误分类，不自行增加冻结设计未规定的重复规模。

**通过标准**：当前进程可捕获的错误产生唯一 ExecutionEnded 和稳定 termination reason；不可捕获的进程丢失由 recovery 终结为 interrupted/process_lost；没有长期 running Execution 或 claimed 请求；相同 Execution 只能有一个终结事件。

**失败条件**：异常冒泡后遗留运行态、需要人工 finalize、重复终结，或 Trace 在 Execution 创建前写入。

### BV-15：Checkpoint、崩溃恢复与新 Execution

**设计依据**：ADR-004 第 3、4 节；`TASK_RUNTIME.md` 第 11、12 节；P4-A07/P4-A08 设计目标。

**验收方式**：从 Production 的公开/只读权威事件观察 claim 后/Execution 前、Execution 后/Hermes 前、等待前后、result 保存后/Governance 前、decision 保存后/apply 前等设计冻结的 crash window，并由外部进程真实强杀 Worker。重启后只启动正式服务，由 startup scanner 自动恢复。测试不得插入中间记录、直接调用 recover 或自行增加冻结设计未规定的重复规模。

**通过标准**：orphaned Execution 确定性终结；恢复创建新 Run Request 和新 Execution，不恢复 Python 栈；Checkpoint 只作为持久化上下文信封；已保存 result/decision 幂等复用；不存在半生命周期记录。

**失败条件**：重启后人工干预、继续旧 Execution、丢失 feedback/session refs，或重复应用 result/decision。

### BV-16：外部操作 crash window 与 reconciliation

**设计依据**：`EFFECT_IDENTITY_AND_IDEMPOTENCY.md` 第 7、9 节；`TASK_RUNTIME.md` 第 12.1、12.2 节；P4-A09 设计目标。

**验收方式**：通过 Production 的公开/只读权威事件和实际业务系统观察，识别 prepared 提交前后、实际网络 dispatch 前后、远端已创建但本地未 acknowledged、acknowledged 后未 observation、observation 后未 confirmed 等冻结 crash window，再由外部进程强杀 Worker。重启后只启动正式 recovery/reconciliation，通过实际业务 API 查询；测试不得预写 operation 状态、直接调用 reconcile 或伪造远端返回。

**通过标准**：只有可证明 `confirmed_not_dispatched` 的 prepared operation 才可用同一 identity 继续；任何可能已 dispatch 的状态先对账；最终最多一个业务对象；不能确认时稳定为 indeterminate 并阻止 completion。

**失败条件**：重启直接重发、用新的 idempotency key、把 response loss 判失败后再建新对象，或用 Tool Result 代替远端 observation。

### BV-17：多进程 lease、takeover 和 fencing

**设计依据**：ADR-004 第 5 节；`TASK_RUNTIME.md` 第 14 节；P4-A10/P4-A11 设计目标。

**验收方式**：按设计部署两个或更多独立 Worker 共享同一本地 SQLite，并通过真实调度竞争同一 Run Request；通过停止 heartbeat、网络隔离或挂起真实旧 Worker 形成 lease 到期和 takeover，随后恢复旧 Worker 使其自然尝试 heartbeat、Tool dispatch、result 和 finalize。测试不得直接伪造 lease token、调用内部 finalize 或自行增加冻结设计未规定的重复规模。

**通过标准**：同一时刻只有一个有效 token；active lease 不被提前接管；takeover 产生新 token；旧 token 的所有状态写和新副作用都被拒绝；最终只有一个有效 Execution 结果和一个业务效果。

**失败条件**：只有进程内锁、没有 token 校验、旧 Worker 能覆盖新 Worker，或并发下出现双 dispatch。

### BV-18：Facts、Outbox、Rules 和启动重投

**设计依据**：`RELIABILITY_FACTS_AND_EVIDENCE.md` 第 5—8 节；`TASK_RULES.md`。

**验收方式**：将 Outbox Dispatcher 作为独立 Production 进程运行，在权威状态已由 Production 提交且 Dispatcher 尚未消费时从进程外强杀相关服务，恢复后验证正式 Dispatcher 幂等重投。通过真实业务执行自然形成跨 Attempt 无进展、迟到 confirmed operation、恢复分歧和 receipt/business mismatch，由 Production Rule runner 自行求值；测试不得写 Fact、Outbox 或 finding。

**通过标准**：权威状态与 Fact/Outbox 原子一致；未投递记录重启后最终投递且不重复产生业务后果；Rule 只读固定 Snapshot/Fact window，输出稳定 finding/indeterminate；Policy 独立决定动作。

**失败条件**：Outbox 丢失、重复投递改变业务状态、Rule 直接写 Task/调用 Tool，或真实 Gateway 行为没有对应事实。

### BV-19：稳定性量化验收的设计缺口

**设计依据**：`DESIGN GAP`。冻结设计没有规定稳定性量化验收标准。

**验收方式**：当前不开展稳定性量化验收。需设计方补充正式设计后，再开展对应整改和验收。

**通过标准**：当前不存在可由冻结设计导出的通过标准，本项不得判定通过。

**失败条件**：任何使用自行规定的次数、时长、任务规模、故障比例、恢复时限或资源上限判定通过的结果均无效；当前结论只能是 `DESIGN GAP`。

### BV-20：设计契合度、状态声明和发布裁决

**设计依据**：Authority Manifest 的 P0/P1/P2 优先级及全部冻结文档。

**验收方式**：为 BV-01—BV-18 建立可机读 traceability manifest，逐项记录设计章节、代码入口、运行配置、业务场景、权威证据文件、实际业务查询结果、判定和未解决差异；BV-19 必须如实记录为 `DESIGN GAP`。由未参与实现的审计者从原始证据重新裁决，不读取 pytest 结果。

**通过标准**：BV-01—BV-18 的实际裁决和证据被完整、准确记录；BV-19 保持 `DESIGN GAP`，S-15 的设计冲突保持 `BLOCKED`；所有未解决问题均未被包装成通过或完成声明。代码行为与文档不存在未声明漂移，README、PHASE_STATUS 和发布说明如实反映当前完成度。

**失败条件**：任何阻断或设计缺口被隐藏、弱化或标绿；使用测试预期解释设计；缺少 Production 权威证据仍声明 Phase 3 production consistent、Phase 4 complete/frozen 或 production ready。

## 3.3 验收产物要求

最终验收包至少包含：

```text
acceptance-manifest.json
design-traceability.json
runtime-composition.json
schema-and-migration-report.json
authoritative-state-export/
business-system-observations/
command-idempotency-report.json
contract-and-gateway-report.json
effect-and-approval-report.json
completion-and-snapshot-report.json
crash-recovery-report.json
reconciliation-report.json
multi-process-fencing-report.json
queue-and-budget-report.json
facts-outbox-rules-report.json
process-logs/
database-files/
```

每个报告必须记录 Git commit、配置摘要、实际 Hermes 版本、Provider 标识、业务 sandbox/tenant 标识、数据库 schema version、开始/结束时间、执行命令、动态业务对象引用、原始证据路径和裁决理由。敏感凭证不得写入报告，但必须保留可审计的受保护引用。

pytest、coverage、ruff 和普通 CI 输出可以作为研发辅助附件，但不得进入 `acceptance-manifest.json` 的通过依据，也不得替代任何 BV 条目的真实业务证据。

## 3.4 最终发布门槛

S-15 的 `DESIGN CONFLICT / BLOCKED` 或 P-18/BV-19 的 `DESIGN GAP` 未解除时，不允许发布完成声明。设计责任方完成裁决或补充前，本合同不预设解决方式，也不定义对应通过标准。

其余已具备冻结设计依据的整改只有同时满足以下条件，才可声明对应能力完成：

1. 对应整改已经进入唯一 Production 运行链；
2. 对应 BV 条目由 Production 权威证据证明通过，不存在“无法验证”或测试补偿；
3. 实际业务系统状态与 Task 终态逐项一致；
4. crash、restart、concurrency、cancel、approval 和 reconciliation 的冻结安全不变量均无违例；
5. 权威文档、代码、运行配置和验收 manifest 相互一致。

在此之前，允许的最高表述是“整改中”或“具备局部开发能力”；不得表述为 Phase 3 生产全量一致、Phase 4 闭环完成、COMPLETE AND FROZEN 或 production ready。

## 3.5 Testing Philosophy 一致性审计与追踪矩阵

本文已按“设计 → Production → 测试”的顺序重新检查全部测试相关内容。问题、Production 整改和验收条目之间的强制对应关系如下：

| 问题 | Production 整改 | 设计驱动的业务验收 |
|---|---|---|
| P-01 双权威与生命周期断链 | S-01、S-05、S-06 | BV-01、BV-02、BV-07、BV-08 |
| P-02 Contract 未强制 | S-02 | BV-04 |
| P-03 副作用治理绕行 | S-03 | BV-05、BV-06、BV-16 |
| P-04 cancel/迟到写 fail-open | S-04 | BV-09 |
| P-05 waiting_input 不可恢复 | S-05 | BV-07 |
| P-06 waiting_approval 不可恢复 | S-05 | BV-06 |
| P-07 Policy action 与 limits 不闭环 | S-06 | BV-08、BV-13 |
| P-08 Completion false success | S-07 | BV-10 |
| P-09 Snapshot 不可稳定重放 | S-08 | BV-11 |
| P-10 生产 registry 缺失 | S-09 | BV-12 |
| P-11 队列毒化 | S-10 | BV-13 |
| P-12 Executor/Worker 异常悬挂 | S-10、S-11 | BV-01、BV-14 |
| P-13 recovery/checkpoint 缺失 | S-12 | BV-15、BV-16 |
| P-14 lease/fencing 缺失 | S-13 | BV-17 |
| P-15 Facts/Outbox/Rules 无运行闭环 | S-14 | BV-18 |
| P-16 测试反向塑形 Production | S-15 | BV-01—BV-20 的统一驱动边界 |
| P-17 状态声明失真 | S-15 | BV-20 |
| P-18 稳定性标准缺少设计依据 | S-16 | BV-19 |

一致性审计后的结论为：

- 全部 BV 条目都明确引用需求或冻结设计，当前代码和历史测试只用于识别缺陷，不再用于定义预期；
- 全部 BV 验收方式都要求通过 Production 公开入口或真实进程/依赖边界驱动，禁止测试代建生命周期和业务状态；
- 原先无设计依据的固定重复次数、时长和任务数量及其派生要求已删除；稳定性量化验收保持 `DESIGN GAP`；
- HermesExecutor、registry 等争议语义不再允许按当前实现解释，必须服从现有设计，或先完成正式设计变更；
- 设计缺失、真实环境缺失和无法验证均保留为非通过状态，不允许转成“部分符合”或绿色结果；
- pytest、fixture、coverage 和历史通过率只允许作为研发线索，不得进入发布裁决证据。

因此，本方案对 Testing Philosophy 的执行不是以第 3.0 节的文字声明为限，而是已经落实到问题定性、整改顺序、每个验收场景的驱动方式、证据来源、失败条件、稳定性标准和最终发布门槛中。
